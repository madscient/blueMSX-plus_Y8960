# Y8960 エミュレーション 実装計画（blueMSX+）

作成: 2026-09-12 / 最終更新: 2026-09-12
状態: **Y8960 のデバイスはまだ 1 つも無い。ビルド環境は通っており、
その前提として上流の I/O ポートを重ね合わせ対応に直した（§4.2、起動確認は未了）**

Y8960 のハードウェア仕様は `hardware-notes.md`（madscient/openMSX_Y8960 からの
写し）。**ただし写しより新しい情報が §3.3 にある。そちらを先に見ること。**
本書はそれらを前提に、**blueMSX+ 側をどう作るか**を決める文書である。
決定・却下・訂正はすべてここに追記する。

## 確度の読み方

本書の記述には確度を付ける。

- **確認済み** — 走らせて確かめた。何でどう確かめたかを添える
- **確認済み(読解)** — 当該ファイルを読んで確認した。ファイル名と行を示す
- **未検証** — 読解して組み立てた設計だが、動かして確かめていない
- **推測** — 出典を示せない。根拠を一行添える

**Y8960 に関する記述に「確認済み」は一つも無い。実装が無いので当然である。**
「確認済み」が付いているのはビルド環境と blueMSX+ の既存の挙動についてだけで、
それも `doc/fork/build/README.md` に集めてある。

---

## 1. 目的とスコープ

Y8960 カートリッジ（hra1129/Y8960_Cartridge）を blueMSX+ でエミュレートする。

**Y8960 を構成するデバイスブロックは、個別にマシン構成へ追加できるようにする**
（2026-09-12 ユーザー指示）。カートリッジ一体ではなく、ブロックごとに独立した
`RomType` を持たせ、`Machines/<名前>/config.ini` の `[Slots]` 行に 1 行ずつ
書けるようにする。理由は Y8960 のハードウェアがまだ WIP であり、
**どのブロックが最終的にどう組み合わさるかが決まっていない**こと。

対象ブロックは 7 つ。

| ブロック | 相当チップ | 備考 |
|---|---|---|
| OPLLEX | YM2413 + チャンネル別音色バンク | 2 回路 |
| OPL2EX | YM3812 + ADPCM-B | 2 回路。レジスタ配置は Y8950 と同一 |
| SSGS | YMZ705/732 相当 | 内部に YM2149 相当が 2 系統、チャンネルごとパンポット |
| MSX-TIMER | 独自 | 4 本のカウンタ + 割り込み |
| MSX-MIXER | 独自 | ブロックごとの左右ゲイン |
| Y8960 SCC | Konami SCC + 独自マッパー | バンクメモリを含む |
| DCSG | SN76489 | 2 回路。**音源コアは blueMSX に既存** |

### スコープ外

- FPGA コンフィグ・SPI Flash・SRAM 初期化シーケンス。実機の起動手順であって、
  エミュレータでは初期化済みで持てばよい
- ADPCM メモリの構成切り替え（**実機側で設定方法が未実装**）。
  ただし**器は 4 状態ぶん作る**。実装するのは起動時の共有モードだけ（§5.1）
- BIOS ROM イメージ（存在しない）

## 2. 前提

以下が変わったら計画を見直す。

- **Y8960 は WIP であり、仕様は動く。** 実機も BIOS ROM も存在しない。
  したがって「実機と同じ音」は検証できない。目標は
  「仕様書と RTL の読解結果、および openMSX_Y8960 の実装と一致すること」まで
- 一次情報の優先順位は **hra1129 さんの直接の回答 > xlsx/docx > RTL**
  （`hardware-notes.md` 冒頭）。**blueMSX+ 側では一次情報に当たり直していない**
- blueMSX+ 全体のライセンスは **GPLv2**（**確認済み(読解)**: ルートの
  `README.md` §License）。持ち込めないものは §3.1

## 3. 情報源と、それぞれの役割

| リポジトリ | 役割 | 扱い |
|---|---|---|
| hra1129/Y8960_Cartridge | Y8960 の**一次仕様**（FPGA RTL + docx/xlsx）。WIP | 仕様の参照元。**コードもデータも取り込まない**（§3.1） |
| madscient/openMSX_Y8960 | openMSX 側の Y8960 実装。全ブロック実装済み・実測済み。GPL-2.0 | **移植元**。設計判断と確定値の出所 |
| madscient/Y8960emu | OPLLEX / OPL2EX の別実装（ymfm ベース）。MIT | 設計の参照と挙動の突き合わせ用。コアは移植しない |
| madscient/EPSGemuEngine | YMZ705/732/771 系 SSG の実装。MIT + BSD-3 | SSGS のレジスタ配置とパンポット則の出所 |
| madscient/MsxSoundSuiteExtension | **Y8960 を駆動する拡張BASIC と BIOS**（MSSX）。開発中 | **ファームウェアが前提にしている仕様の出所**（§3.3）。`y8960bas.rom` は検証に使える（§8.3）。**ROM をリポジトリに入れてはいけない**（§3.1） |
| buppu3/openMSX | `y8960` ブランチに Y8960 実装。GPL | openMSX_Y8960 の土台。blueMSX+ からは直接参照しない |

### 3.1 持ち込んではいけないもの

**hra1129/Y8960_Cartridge の FPGA ソースと図表。**
そのライセンスは条項 3 で「書面による事前の許可なしに販売、および
商業的な製品や活動に使用しないこと」を課しており、**非商用制限は GPL と両立しない**
（出所は openMSX_Y8960 のルート `CLAUDE.md` §1。**blueMSX+ 側で原文に当たり直しては
いない**）。仕様を参照するのは構わないが、この線を越えない。
同じ規則が `doc/fork/README.md` の「ライセンスと帰属」にもある。

**MSSX の ROM イメージ（`y8960bas.rom` など）。**
**再配布には MSX ライセンシングコーポレーションの許諾が別途必要**で、
許諾されているのは個人利用に限られる（**確認済み(読解)**: MSSX の `NOTICE.md`）。
blueMSX+ は GPLv2 で配布されるので、同梱すれば配布物に再配布制限付きの
バイナリが入ることになる。

**ローカルで検証に使うのは個人利用の範囲内である。** 実機の BIOS ROM と
同じ扱いで、gitignore 済みのビルド出力に置く（`doc/fork/build/README.md` §4）。

`hardware-notes.md` は**読解した事実の記述**であって、コードや文章の複製ではない。

### 3.2 落としてはいけない帰属表示

音色データを持ち込む場合、出所のライセンス表示が要る。詳細は §5.2。

**"Copyright free OPLL(x) ROM patches"（David Viens / Hubert Lamontagne、
CC BY-SA）は MSSX 側でも使われている**（**確認済み(読解)**: MSSX の `NOTICE.md`。
MSX-AUDIO BASIC Extension Lite のリズム音色 3 本）。
§5.2 で OPLL-X の音色データの調達先として挙げているのと同じ出典である。
どちらから採っても帰属表示が要る。

### 3.3 写しより新しい情報 — ファームウェアが前提にしている仕様

**madscient/MsxSoundSuiteExtension（MSSX）は Y8960 を駆動する拡張BASIC と
BIOS である。** その `docs/y8960/hardware.md` が「本ファームウェアが依存する
範囲」を書いており、**実際に動くソフトが前提にしている仕様**なので、
`hardware-notes.md`（写し、2026-09-08 の調査）より新しく、かつ強い。

以下すべて **確認済み(読解)**（2026-09-12 に同文書を読んだ）。
**走らせて確かめてはいない**（**未検証**）。

#### 決着が付いたもの

| # | 論点 | 写しでの状態 | MSSX の記述 |
|---|---|---|---|
| 1 | **ROM / RAM バンクの境界** | 出典 4 つが 4 つとも違い、RTL にはバンク番号による分岐が無い（§7.3） | **256KB、`#0`-`#15` が ROM、`#16`-`#31` が RAM**。「RAM バンクを出しても初期状態では書き込みは効かない」 |
| 2 | **SCC 窓の位置** | RTL は 9800h と、RAM モード限定の 7800h | **BANK1 のバンクレジスタに `3Fh`。窓は `7800h`** |
| 3 | **MMIO 窓の出現条件** | RTL は `memory_io_en = ~rammode` | **BANK1 に ROM バンクが出ているときだけ。RAM バンクまたは SCC のときは隠れる**（2026-09-12 にユーザーが確定。§9.5） |
| 4 | **ADPCM メモリの構成** | 切替レジスタが無く未決（§7.5） | **4 状態ある（共有 / 片寄せ ×2 / 分割）。設定方法は実機側で未実装。暫定で起動時は共有**（2026-09-12 にユーザーが確定）。MSSX も共有前提。§5.1 |
| 5 | **OPLL の I/O Enabler のビット** | RTL と xlsx が逆（§6.1） | **b0 = OPLL1 (7Ch-7Dh)、b1 = OPLL2 (7Ah-7Bh)** |

**#5 は openMSX_Y8960 の判断が正しかったことの裏付けである。** あちらは
RTL を取り違えとして採らず、FM-PAC を正として b0 に 7Ch-7Dh を割り当てた。
ファームウェアも同じ割り当てで書かれている。

**#2 も同じ形で裏付けられた。** 従来の実装は SCC を 9800h にしか出しておらず、
openMSX_Y8960 が 4 つの region すべてに広げた（あちらの §3.8.1 規則 1）。
**ファームウェアは 7800h を使うので、9800h だけの実装では動かない。**

**#3 は openMSX_Y8960 の規則 2 と一致する。** あちらは
`!(ramEnabled && isRamRegion(3)) && bankreg1 != 3Fh` と書いており、
**BANK1 が ROM のときだけ窓を出す**という同じ条件になる。

**訂正**: 本書は当初、MSSX の文を「RAM モード 0 のとき**または** BANK1 に ROM」
という 2 条件の or と読み、RAM モード 0 で BANK1 が SCC のときに食い違うと
書いた。**これは誤読だった。** 2026-09-12 のユーザーからの回答で、
**見るのは BANK1 の中身だけ**と確定した（§9.5）。

#### 新しく分かったこと

| | |
|---|---|
| **クロック** | SSGS は **1.7897725MHz**（`3.579545 / 2`）、OPLL-EX / OPL2-EX / DCSG / SCC は **3.579545MHz**。「Y8960 は MSX 標準音源のアップグレードパスなので、置き換える相手と同じクロックが配られる」。**一次資料に数値の記載は無く、設計意図として確認されたもの**（MSSX 文書の但し書き） |
| **SSGS の LED** | **SSG2 のレジスタ `2Fh`** の下位 4bit。写しは旧 RTL に基づき `1Fh` と書いている |
| **SSGS の系統振り分け** | 「レジスタアドレスが `13h` 未満なら SSG1、`20h` 以上なら SSG2」。写しの `(reg >> 5) & 1` と実質同じ（`$13`-`$1F` は未定義領域） |
| **OPLL-EX の音色レジスタ** | `03h` の bit4-3 が DC / DM（波形選択）。素の YM2413 と同じ |
| **BIOS ROM が実在する** | `y8960bas.rom` **128KB**。ROM バンク `#0`-`#15` にちょうど収まる。§8.3 |

#### MSSX 側にも未確定として残っているもの

**エミュレータ側で決めても、ファームウェアと突き合わせられない。**

- Digital Mixer のレジスタマップ（仕様書の「レジスタ一覧」が未記入）
- **ADPCM メモリの構成切り替え方法** — **実機側で未実装**であることが
  2026-09-12 に確定した。暫定で起動時は共有モード（§5.1）
- 推奨初期化シーケンス、レジスタ書き込みの最小ウェイト

**§5.6 の「MSX-MIXER は値を憶えるだけにする」判断は、これで裏付けられた。**
ファームウェア側も同じ理由で待っている。

## 4. blueMSX+ の作法（調査結果）

すべて **確認済み(読解)**。2026-09-12 に当該ファイルを読んだ。

### 4.1 デバイスの足し方

blueMSX のデバイスは `RomType`（`Src/Media/MediaDb.h` の enum、現在
`ROM_MAXROMID = 179`）で識別される。1 ブロックを足すのに触るのは次の 6 か所。

| 場所 | 何を足すか |
|---|---|
| `Src/Media/MediaDb.h` | enum の値。`ROM_MAXROMID` も更新 |
| `Src/Media/MediaDb.cpp` | `mediaDbStringToType`（DB の XML 名 → enum）、`romTypeToString`（表示名）、`romTypeToShortString`（短縮名） |
| `Src/Board/Machine.c` | 生成。ROM を伴わないデバイスは前半のループ、伴うものは後半の `switch` |
| `Src/Memory/romMapper<名前>.c` | 実装本体 |
| `Src/Win32/Win32machineConfig.c` | マシン構成エディタの一覧と、ROM ファイルを要求するかどうかの分岐 |
| `Src/Language/` | `romTypeToString` が返す表示名の文字列 |

**`config.ini` に書かれるのは enum の数値**である（**確認済み(読解)**:
`Machines/*/config.ini` の `[Slots]` 行は
`スロット サブスロット 開始ページ ページ数 romType "ROMファイル" ""` の形）。
したがって数値を後から変えると、既に作ったマシン構成ファイルが全部ずれる。

**ROM を伴わないデバイスの最小形**は `Src/Memory/romMapperTurboRTimer.c`（135 行）が
そのまま雛形になる。`deviceManagerRegister(romType, &callbacks, ref)` で
destroy / reset / saveState / loadState を登録し、`ioPortRegister` でポートを取る。

### 4.2 I/O ポートの重ね合わせ — **上流を直した（2026-09-12）**

**直す前の状態**（**確認済み(読解)**）。`ioPortRegister` は 1 ポートに 1 デバイス
しか登録できず、**既に誰かが登録していると黙って無視していた**。
戻り値が無いので呼び出し側は気づけない。

Y8960 は**設計上、本体デバイスと同じポートに重なる**ブロックを持つ。

| ポート | Y8960 | 本体側 | 重なりの意図 |
|---|---|---|---|
| A0h-A2h | SSGS | PSG | **設計どおり**。カートリッジ版 SSGS は本体 PSG に重ねて発音するだけの存在で、読み出しは本体 PSG が担う（`hardware-notes.md` §2） |
| 7Ch-7Dh | OPLL #1 | MSX-MUSIC | 機種による |
| C0h-C3h | OPL2 #1/#2 | MSX-AUDIO | 機種による |

しかも `Src/Board/MSX.c:253-259` は **`machineInitialize()` でスロットのデバイスを
作ってから `msxPsgCreate()` を呼ぶ**ので、マシン構成に置いた Y8960 SSGS は
本体 PSG より先に登録され、**本体 PSG の A0h / A1h 登録が黙って落ちていた**はずだった。

**2026-09-12 のユーザー判断により、これをエミュレータの不具合として上流を直した。**
実機のバスでは複数のデバイスが同じポートに応答しうる。

#### 直した内容

`Src/Memory/IoPort.c` と `IoPort.h`。

| | |
|---|---|
| 登録 | 1 ポートあたり **`IO_PORT_MAX_CLAIMS` (= 4) 件**まで持てる。同じ `ref` の再登録は上書き |
| 書き込み | **登録されたすべてに配る** |
| 読み出し | **駆動するものすべての AND**。実機で複数のカードが同時に応答したときと同じ |
| 解放 | **`ioPortUnregister(port, ref)` に `ref` を足した。** 自分の登録だけが消える |

**ハンドラの実行中に構成が変わりうる**（I/O イネーブラがまさにそれをする）ので、
配る前にエントリをローカルへ写してから呼ぶ。

`ioPortRegister` の呼び出し側（207 箇所）は**無変更**。
`ioPortUnregister` は**シグネチャが変わったので 53 ファイル 200 箇所を直した**。
`ref` は各 destroy 関数の中にあるデバイスポインタで、
**同じファイル内で同じポートを登録している行の第 4 引数**から機械的に決めた。

#### 直した副作用として、既存の不具合が 3 件見つかった

**どれも「1 ポート 1 デバイス」の下では実害が出にくく、重ね合わせを許すと
実害になる**もの。いずれも **確認済み(読解)**。

| ファイル | 何が起きていたか |
|---|---|
| `Src/Memory/romMapperKanji12.c` | **他人のポートを解放していた。** 自分は `0xF7` のサブポートしか登録しないのに、destroy で `0xD8`-`0xDB` を解放していた。それは `romMapperKanji` のもの。該当行を削除した |
| `Src/Memory/romMapperOpcodeModule.c` | **登録したポートを解放せず、登録していないポートを解放していた。** 登録は `0x40` / `0x50`-`0x52`、destroy は `0x60`-`0x6F`。登録した側を解放するように直した |
| `Src/IoDevice/Sf7000PPI.c` | `0xE0` / `0xE1` の解放漏れ。2 行足した |

`ioPortGetRef()` で「自分が登録できたか」を確かめてから解放していた 2 箇所
（`romMapperKorean90.c`、`romMapperMsxAudio.c`）は**ガードを外した**。
`ref` で解放するようになった以上そのガードは不要で、**残すと有害**である —
自分が 2 番目の登録だと `ioPortGetRef` は他人の `ref` を返し、
解放されないまま残る。

#### 確度

**ビルドは通っている**（**確認済み**: 0 エラー。`doc/fork/build/README.md` の手順）。

**起動と描画の非回帰は確認した**（**確認済み** 2026-09-12）。
`MSX2+ - Sony HB-F1XDJ` を起動し、**変更前のコミットでビルドした実行ファイルと
同じ画面（MSX2+ の青い BASIC 画面）が出ることを見た**。
変更前と変更後でビルドし直して同じ機種を起動し、
スクリーンショットを撮って見比べている。

**重なりが実際に起きることも確認した**（**確認済み** 2026-09-12）。
`ioPortRegister` に一時的な記録コードを入れてビルドし、4 機種で起動した。

| 構成 | 登録数 | 重なり |
|---|---|---|
| C-BIOS MSX2+ | 23 | 0 |
| Panasonic FS-A1WSX | 27 | 0 |
| **同 + FM-PAC** | 29 | **2（7Ch と 7Dh）** |
| Panasonic FS-A1FM | 24 | 0 |

**この記録コードには判別力がある** — 重なりの無い構成では 0 件、
FM-PAC を挿した構成でだけ 2 件出た。**変更前は FM-PAC 側の登録が
黙って落ちていた**ことになる。記録コードはコミットしていない。

**書き込みが本当に両方へ配られることを実測した**（**確認済み** 2026-09-12）。
`romMapperFMPAC` と `romMapperMsxMusic` の書き込み口に一時的な記録コードを入れ、
`MSX2+ - Sony HB-F1XDJ` に FM-PAC を挿して `CALL MUSIC` で演奏した。

| 記録 | 件数 |
|---|---|
| 本体 MSX-MUSIC に届いた書き込み | **258** |
| FM-PAC に届いた書き込み | **258** |

**完全に同数で、ログ上も交互に並ぶ。** 7Ch / 7Dh への書き込みが
両方のデバイスに配られている。**変更前はこの行が 1 件も出ない**
（FM-PAC の登録が落ちているため）。

**FM-PAC 側は 258 件すべてが `enable=00` だった。** 届いてはいるが、
FM-PAC が自分のイネーブラで捨てている。鳴っていたのは本体 MSX-MUSIC だけで、
**これは FM-PAC の仕様どおりの動作である**（`romMapperFMPAC.c` の
`writeIo` は `rm->enable & 1` のときだけ YM2413 へ流し、リセット時は 0）。

**この構成で FM-PAC のイネーブラが立たなかったのは多重化とは別の話**で、
`CALL MUSIC` が本体側の BASIC を呼んだためと見られる（**未確認** —
どちらの FM-BIOS が選ばれたかを追ってはいない）。

記録コードはコミットしていない。

**まだ確かめていないこと**。**音の善し悪しと、キー入力の操作感**。
音は聞けず、画面は Direct3D 12 で描かれるため中身をキャプチャできない
（`doc/fork/build/README.md` §3.3 / §5）。
ただし **MSX BASIC 3.0 が起動し、Disk BASIC も動き、MSX-MUSIC の演奏が
できている**ところまでは確認できている（**確認済み**: 画面の写しと、
上記 258 件の書き込み）。

### 4.3 サウンドの出し方

`mixerRegisterChannel(mixer, audioType, stereo, updateCb, rateCb, ref)` で登録する
（**確認済み(読解)**: `Src/SoundChips/AudioMixer.h:108`）。`stereo` に 1 を渡せば
ステレオのデバイスにできる（`AY8910.c:281` が実例）。

**blueMSX のミキサーには、デバイス単位のゲイン／パンを外から与える API が無い。**
`mixerSetChannelTypeVolume` / `mixerSetChannelTypePan` はいずれも**チャンネル種別**
単位である（**確認済み(読解)**: `AudioMixer.h:84-88`）。openMSX の
`MSXMixer::setDeviceGain()` に当たるものが存在しない。

したがって **MSX-MIXER のゲインは、各ブロックが自分の出力に自分で掛ける**形になる。
§5.6 に設計を書く。

### 4.4 ビルド定義

ファイルを足したら 3 か所に反映する（**確認済み(読解)**: `blueMSX/Make/` 配下）。

| 場所 | |
|---|---|
| `Make/msvc2022/blueMSX.vcxproj`（+ `.filters`） | MSVC 2022 |
| `Make/msvc2026/blueMSX.vcxproj`（+ `.filters`） | MSVC 2026 |
| `Make/gcc/` | GNU make |

**どれも明示列挙である。自動収集は無い。** `Make/gcc/Makefile` は
`SOURCE_FILES += <ファイル名>` を並べる形で、ディレクトリは `vpath` で解決している
（**確認済み(読解)**: 同ファイル 91行目と 105行目以降）。vcxproj も列挙。
つまり**ファイルを 1 本足すごとに 3 か所（.filters を数えれば 5 か所）に書き足す**。

**CI が通しているのは `msvc2022`（v143）だけである**（**確認済み(読解)**:
`.github/workflows/ci.yml` が `blueMSX/Make/msvc2022` で msbuild を呼ぶ）。
ローカルもそれに揃えてある。`msvc2026`（v145）と GNU make は
**このフォークで試していない**ので、足したファイルがそちらで通るかは分からない。
手順は `doc/fork/build/README.md`。

## 5. ブロックごとの実装方針

すべて **未検証**（設計であって、書いても動かしてもいない）。
土台として挙げたファイルを読んだことは **確認済み(読解)**。

### 5.1 OPL2EX（YM3812 + ADPCM-B）×2

**土台は `Src/SoundChips/OpenMsxY8950Latest/` のフォーク。**

blueMSX+ は openMSX の最新 Y8950 を既に移植して持っている
（**確認済み(読解)**: `OpenMsxY8950Latest.h` の冒頭が
「Ported from openMSX's latest Y8950 (MSX-Audio)」と書いており、
`OpenMsxY8950Latest/` に openMSX 側の原本 `Y8950.cc` / `Y8950Adpcm.cc` が
参照用として置かれている）。

**openMSX_Y8960 の `Y8960OPL2` は、まさにその openMSX `Y8950` のフォークである**
（`hardware-notes.md` 側ではなく openMSX_Y8960 の implementation-plan §3.4）。
したがって **blueMSX+ 側に当てるべき差分は、openMSX 側で既に作られている差分と
同じ**になる。

当てる差分は 3 つ。

| | 内容 |
|---|---|
| 波形選択 | `sinTable` から 4 面のテーブルを作り、スロットが `wave` と `waveTable` を持つ。E0h-F5h がスロットごとに選び、TEST レジスタ 01h の b5 が全体のゲート。リズム用スロットはサインを直接使う |
| 落とすレジスタ | キーボードコネクタ、13bit DAC、`Y8950Periphery`。05h, 06h, 0Dh, 0Eh, 15h-17h, 18h, 19h, 1Ah, 07h の b3 |
| ADPCM | `OpenMsxY8950LatestAdpcm` を共有する。openMSX 側は `Y8950Status` インターフェースを切り出して 2 チップで共有した。同じ手が使えるはず（**未検証**） |

#### ADPCM のサンプル RAM は 4 状態をとる

**2026-09-12 にユーザーから直接の回答があった**（最も強い出典）。

256KB を 2 回路でどう分けるかに **4 つの状態がある**。

| 状態 | |
|---|---|
| **共有** | 2 回路が同じ 256KB を見る |
| **片寄せ（回路 1）** | 256KB を回路 1 に割り当てる |
| **片寄せ（回路 2）** | 256KB を回路 2 に割り当てる |
| **分割** | 128KB + 128KB |

**設定の方法は実機側で未実装である。** どのレジスタで切り替えるかが決まって
いない（写しの §7.5 が「manual にあるがレジスタが無い」と書いていたのは、
この状態を指していたことになる）。

**起動時は共有モードという前提を置く**（ユーザーの暫定判断。
ハードウェア側で決まり次第、修正する）。**MSSX も共有モードで動く前提**である。

**したがって器は 4 状態ぶん作る。** 実装するのは共有モードだけだが、
サンプル RAM を**回路の外に 1 つ持ち、各回路が見る範囲をモードで決める**形に
しておけば、切り替えレジスタが決まったときに足すのは分岐 1 つで済む。
回路ごとに RAM を持たせてしまうと、そのときに作り直しになる。

**やり直しの値段**: 器を先に作れば、モードの追加は分岐 1 つと、
切り替えレジスタを受ける口。器を作らなければ、`Y8960OPL2` 2 つの
メモリ保持を組み替えることになる。

**openMSX_Y8960 とは違う。** あちらは `<sampleram>256` をブロックごとに
持たせており、2 回路が別々の 256KB を見る（どの状態にも当たらない）。

`OpenMsxY8950LatestAdpcm` が RAM を自前で確保しているなら、
外から与える形に変える必要がある。**未確認**（該当箇所を読んでいない）。

**クロックは 3.579545MHz**（§3.3）。

**バックエンド切替の対象にはしない。** blueMSX+ の Y8950 は
`Y8950BackendBase` の下に複数実装を並べてユーザーが切り替えられる作りだが
（**確認済み(読解)**: `Y8950MultiBackend.h`）、Y8960 の OPL2 は 2 回路あり、
切替の器を 2 つ持つ意味が薄い。**独立したデバイスとして作る。**
OPLLEX を emu2413 1 本に決めた判断（§5.2）と揃えた。

**前提**: この判断は「Y8960 が WIP で、音質の比較検討より先に動くことが要る」
段階であることに依っている。仕様が固まって A/B 比較が要るようになったら見直す。
**やり直しの値段**: `Y8950BackendBase` を継承する形に変え、
`Y8950MultiBackend` に相当する器を Y8960 用に 1 本作る。デバイス側は変わらない。

### 5.2 OPLLEX（チャンネル別音色バンク付き YM2413）×2

**土台は `Src/SoundChips/Emu2413/` のフォーク**（2026-09-12 ユーザー決定）。

改造は 2 点。

| | 内容 |
|---|---|
| 音色テーブルを 4 面持つ | 現在の `opll->patch[19*2]` を `[4][19*2]` に広げる |
| チャンネルごとにバンクを選ぶ | `set_patch()` がそのチャンネルのバンクを見る。新設レジスタ 40h-48h の b1:b0 が BANK |

`set_patch()` は `MOD(opll,ch)->patch` / `CAR(opll,ch)->patch` にポインタを
向けているだけなので（**確認済み(読解)**: `emu2413.c:641-648`）、
向ける先をバンクで選ぶだけで済む。**構造上の障害は無い。**

#### **問題: emu2413 に YM2423 (OPLL-X) の音色データが無い**

**確認済み(読解)**: `emu2413.h:16` の `OPLL_TONE_ENUM` は
`OPLL_2413_TONE` / `OPLL_VRC7_TONE` / `OPLL_281B_TONE` の 3 つだけで、
`emu2413.c:35` の `OPLL_TONE_NUM` も 3。

Y8960 の OPLLEX は 4 バンクを要求する（`hardware-notes.md` §6 OPLLEX）。

| BANK 値 | 音色セット | 相当チップ | emu2413 に在るか |
|---|---|---|---|
| 0 | OPLL | YM2413 | **在る** (`OPLL_2413_TONE`) |
| 1 | OPLL-X | YM2423 | **無い** |
| 2 | OPLL-P | YMF281 | **在る** (`OPLL_281B_TONE`) |
| 3 | VRC7 | DS1001 | **在る** (`OPLL_VRC7_TONE`) |

調達先の候補。**どれも未確定**（§9.2）。

1. **`Src/SoundChips/NukedOPLL/opll.c` の `patch_ym2423`**（**確認済み(読解)**:
   同ファイル 189 行目に在る）。同じリポジトリ内で、ライセンスは GPLv2
   （**確認済み(読解)**: `NukedOPLL/LICENSE`）。`opll_patch_t` 構造体から
   emu2413 の 8 バイトダンプ形式への変換が要る
2. **openMSX_Y8960 の `YM2413NukeYKTBanked`** の音色データ。出所は
   "Copyright free OPLL(x) ROM patches" (David Viens / Hubert Lamontagne)、
   **CC BY-SA なので帰属表示が要る**

**ライセンスの注意**: emu2413 は MIT（**確認済み(読解)**: `Emu2413/LICENSE`）。
そこに GPLv2 のデータを入れると、**フォークしたファイルは GPLv2 になる**。
blueMSX+ 全体が GPLv2 なので配布上の問題は生じないが、
**Okazaki 氏の MIT 表示は残し、追加分の出所も併記する。**

**やり直しの値段**: 音色データ 1 テーブル（19 音色 × 8 バイト）の差し替え。
コードは変わらない。**先に決めなくても実装を始められる** — 1 バンクを
暫定で OPLL と同じにしておき、後で表だけ差し替えればよい。

### 5.3 SSGS（YMZ705/732 相当）×1

**土台は `Src/SoundChips/AY8910.c` のフォーク**（582 行）。

| | 内容 |
|---|---|
| レジスタ配置 | `$00-$1F` が SSG-1、`$20-$3F` が SSG-2。系統の選択は `(reg >> 5) & 1`。各系統内は `sub = reg & 0x1F` |
| `$0E-$0F` | **無い**。YMZ 系は I/O ポートを持たない（カートリッジ版の場合） |
| `$10-$12` | ch A/B/C の 4bit パンポット |
| 出力 | 1 つのステレオ・サウンドデバイス（6ch）。`mixerRegisterChannel(..., stereo=1, ...)` |

**クロックは 1.7897725MHz**（`3.579545 / 2`。§3.3）。本体 PSG と同じである。

**LED はSSG2 のレジスタ `2Fh` の下位 4bit**（§3.3）。
`hardware-notes.md`（写し）は旧 RTL に基づき `1Fh` と書いているが、
新しいレジスタ配置では `2Fh` になる。

パンポット値と L/R レベルの対応は**データシートに記載が無い**。
EPSGemuEngine（`src/YmzSsg.cpp`）と openMSX_Y8960（`Y8960SsgCore.cc`）は
同世代の YMZ280B と同じ分配則を採っている。**同じ則に合わせる**。
これは実装者の判断であって仕様ではない。

**AY コアの出力は片極性なので、パンを掛ける前に無音時レベル（直流成分）を
差し引く。** 省くと直流が定位に漏れる（出所: EPSGemuEngine のコメント。
openMSX_Y8960 も同じ処置をしている）。

**カートリッジ版はライトオンリー。** `ioPortRegister(port, NULL, write, ref)` で
読み出しを持たない登録ができる（**確認済み(読解)**:
`romMapperFMPAC.c:261-262` が 7Ch/7Dh でそうしている）。

**A0h / A1h が本体 PSG と重なる件は、上流を直して解決した**（§4.2）。
両方が登録でき、書き込みは両方に届き、読み出しは本体 PSG だけが駆動する。
**SSGS の着手を止めるものは無くなった。**

### 5.4 DCSG（SN76489）×2

**音源コアは blueMSX に既存**（`Src/SoundChips/SN76489.c`、650 行）。
2 インスタンスを作り、3Eh → DCSG 0、3Fh → DCSG 1 に載せる
（アドレス bit0 が回路を選ぶ。`hardware-notes.md` §7.1）。
1 バイト書き込みのみのレジスタラッチ方式なのでライトオンリー。

openMSX 側が `SNPSG` に足さず別デバイスに切り出した理由（イネーブラのゲートと
トンネルの入口が要り、既定値が他デバイスと逆になる）は blueMSX+ でも同じなので、
**`Src/Memory/romMapperY8960Dcsg.c` を別に作り、`SN76489` はコアとして呼ぶだけにする。**

**実装済み（Phase 2-1）。** 以下は着手前の見立てで、そのとおりになった。

**`sn76489Create` は署名の変更が要る。** 現在は `sn76489Create(Mixer*)` で、
ミキサー種別が `MIXER_CHANNEL_PSG` に、デバッガの表示名が `"SN76489 PSG"` に
固定されている（**確認済み(読解)**: `SN76489.c:226-227`。
同ファイル 485 行にもう一つ `sn76489Create` があるが、42 行目の `#if 1` /
376 行目の `#else` に挟まれた**死んだ側**である）。

Y8960 の DCSG は専用のミキサー種別（§6）に載せ、2 回路を区別できる名前が要るので、
**種別と名前を引数に取る形へ広げる**。既存の呼び出し側（ColecoVision / SG-1000 /
`SN76489.xml`）は既定値で通るようにする。上流ファイルに手が入るが、
openMSX_Y8960 も同じ理由で同じ変更をしている。

#### 実装（Phase 2-1、2026-09-12）

`Src/Memory/romMapperY8960Dcsg.c`。**音源コアは既存の `SN76489` をそのまま使う。**

`sn76489Create()` は `MIXER_CHANNEL_PSG` とデバッガ名 `"SN76489 PSG"` に
固定なので、**`sn76489CreateEx(mixer, audioType, name)` を足した**
（`Src/SoundChips/SN76489.{c,h}`）。SCC と同じ手で、**呼び出し側 4 か所は無変更**。
Y8960 の 2 回路は `MIXER_CHANNEL_Y8960` に載り、
`"Y8960 DCSG 0"` / `"Y8960 DCSG 1"` として区別できる。

**直接 I/O は書き込み専用で登録する。** SN76489 には読み出すものが無く、
`3Eh` / `3Fh` は本体の何かと重なりうる。

#### 確かめたこと（**確認済み** 2026-09-12）

テスト ROM の `t8:` が、イネーブラの状態を変えながら別々の値を書く。
書き込み口に記録を入れて読んだ結果が次。**出なかった行にも意味がある。**

| 書いた先 | 値 | イネーブラ | 届いたか |
|---|---|---|---|
| `3Eh` | 11h | DCSG0 閉 | **届かない** |
| `3Eh` | 22h | DCSG0 開 | 届いた（ch0） |
| `3Fh` | 33h | DCSG1 **閉**（DCSG0 だけ開けた） | **届かない** |
| `7FF1h` | 44h | — | 届いた（ch0） |
| `7FF0h` | 55h | — | 届いた（ch1） |

**1 回の掃き出しで 5 つ確かめられている。** ゲートが効くこと、開くと届くこと、
**2 回路が別々にゲートされること**、トンネルがゲートを受けないこと、
そしてトンネルの宛先が窓の並び（高位が 1 番目）どおりであること。

記録はコミットしていない。掃き出しはテスト ROM に残してある。

**音そのものは未検証。** 波形が出ているかは耳で聴くしかない。

### 5.5 MSX-TIMER

**新規実装。** 移植元は openMSX_Y8960 の `src/sound/MSXTimer.cc`（517 行）。

| | 内容 |
|---|---|
| ポート | B0h = レジスタ番号、B1h = 値、B2h = 割り込みフラグ、B3h = カウンタ読み出し |
| 分解能 | Base Clock 85.909080MHz (= 3579545 × 24) を `2^(10+2r)` で分周。8 段 |
| 割り込み | `boardSetInt` / `boardClearInt`（**確認済み(読解)**: `Src/Board/Board.h:151-152`） |
| 時間の進め方 | `boardTimerCreate` / `boardTimerAdd`（同 211-213 行） |

**openMSX 側で踏んだ落とし穴を引き継ぐ**: カウント計算が 32bit を溢れると、
分解能 7・終端値 255 でカウンタが張り付く（openMSX_Y8960 の経緯 2026-09-10 (4)）。
**最初から 64bit で計算する。**

**イネーブラで閉じている間もカウンタと割り込みは動き続ける。**
実機のイネーブラが止めるのはアドレスデコードだけで、ブロックを停めるわけではない。

#### 実装（Phase 1-5、2026-09-12）

`Src/Memory/romMapperY8960Timer.c`。**openMSX_Y8960 の `MSXTimer` の移植**で、
数え方はそのまま持ってきた。

**カウンタは分周前のクロックで数える。** 分解能を変えても、カウンタが
どこまで進んでいたかが失われない。読み出すときに `2^(10 + 2 * reso)` で割る。

**桁上がりの手前に起床点を張る**。`boardTimerAdd()` で、分周後のカウンタが
次に 1 増える時刻に起こしてもらう。openMSX の同期ポイントと同じ考え方。

**掛け算は 64bit で行う。** 終端値 255・分解能 7 のとき `256 << 24` になり、
32bit では 0 に落ちる。0 になると起床点が消えてカウンタが止まる
（openMSX 側が 2026-09-10 に踏んだ）。

**止まっている間も時刻だけは進める。** そうしないと、有効にした瞬間に
それまでの経過が一気に流れ込む。

| ポート | 読み | 書き |
|---|---|---|
| B0h | レジスタ番号 | レジスタ番号（bit3-2 がカウンタ、bit1-0 がレジスタ） |
| B1h | 選択したレジスタ | 同左 |
| B2h | 4 本の割り込みフラグ | 立っているビットのフラグをクリア |
| B3h | 選択したカウンタ | カウンタ選択 |

レジスタ 0 は bit0 が繰り返し、bit6-4 が分解能、bit7 が割り込み許可。
レジスタ 1 が終端値。レジスタ 2 は bit0 が動作、bit1 でカウンタをクリア。

**割り込みは `boardSetInt(0x1000)`。** このビットは他のどのデバイスも
使っていない（**確認済み(読解)**: `boardSetInt` の呼び出しを全部見た）。

**イネーブラが閉じている間もカウンタと割り込みは動く。** 実機のイネーブラが
止めるのはアドレスデコードだけなので、閉じるのは I/O ポートの応答だけにした。

#### 確かめたこと（**確認済み** 2026-09-12）

テスト ROM に 3 つ足した（`tests/banktest.asm`）。

| | 内容 |
|---|---|
| 5 | イネーブラを開くまで `B2h` が答えず、`7FFFh` に `80h` を書くと答えるようになる |
| 6 | カウンタが進む |
| 7 | 終端値に達すると割り込みフラグが立ち、`B2h` への書き込みで消える |

**7 つとも OK。判別力も確かめてある** — `y8960IoEnabled()` が常に真を返すように
壊すと、**5 番だけが NG になり、6 と 7 は OK のまま**だった。

**5 番は I/O イネーブラと MMIO 窓の検証でもある。** §5.8 で
「反応するブロックが要る」と書いた宿題が、ここで片付いた。
`7FFFh` への書き込みが窓を通ってイネーブラに届き、
それがタイマーのポートを開けている。

**CPU への割り込みそのものは未検証。** フラグが立つところまでしか見ていない。
割り込みハンドラを置いて数えるテストが要る。

### 5.6 MSX-MIXER

**新規実装。** ただし openMSX 側とは作りが変わる。

§4.3 のとおり blueMSX のミキサーにはデバイス単位のゲイン API が無いので、
**各ブロックが自分の出力に自分で左右ゲインを掛ける。**
MSX-MIXER デバイスは、各ブロックが参照するゲイン値を保持する箱になる。

結線の方法は **未決**（§9.3）。openMSX 側は XML の id 参照で解決していたが、
blueMSX の `config.ini` にそれに当たる仕組みが無い。候補は次の 2 つ。

1. **プロセス内のシングルトン**。`romMapperTurboRTimer.c` が `theTimer` という
   static ポインタを持っているのと同じ形（**確認済み(読解)**: 同ファイル 53 行目）。
   Y8960 は 1 台しか挿さない前提なら成立する
2. **`deviceManager` から型で引く**。既存の API で引けるかは **未確認**

**B6h-B7h はゲインに繋がない**（openMSX 側と同じ）。B6h でレジスタ番号、
B7h でデータ、という入口までは作るが、**レジスタアレイの中身が未定**なので
値を憶えるだけにする。アドレス自体も暫定値である（`hardware-notes.md` §7.4）。

**出力の切り替え**（Y8960 だけ / 本体だけ / 両方）は openMSX 側に在る機能だが、
blueMSX+ でどう出すかは **未決**（§9.4）。

### 5.7 Y8960 SCC（バンクメモリ）

**土台は `Src/SoundChips/SCC.c`（既存）+ 新規マッパー
`Src/Memory/romMapperY8960Scc.c`。**

マッパーの規則は openMSX_Y8960 の implementation-plan §3.8.1 で確定している。
**その 4 規則をそのまま移す。**

| | 規則 | 出所 |
|---|---|---|
| 1 | SCC 音源レジスタは、その region 自身のバンクレジスタが 3Fh のとき、その region の 1800-1FFF に出る。**RAM モードのバンク0 だけは出さない** | openMSX 側の判断。**ファームウェアが使うのは BANK1 の 7800h だけ**なので、残る region は余分に出しているだけ（§3.3） |
| 2 | MMIO 窓は、**バンク1 が ROM を出しているときだけ**出る | **2026-09-12 にユーザーが確定**（§9.5） |
| 3 | RAM モードのバンク0 は書き込み不可 | openMSX 側の既存実装の踏襲 |
| 4 | バンク 3Fh を RAM バンクとして扱わない | バグ修正。`#16` 以上を RAM と判定すると `3Fh` も通ってしまう |

外に出る値は `hardware-notes.md` §3 / §4 と openMSX_Y8960 の §4.4 にある。

| 値 | |
|---|---|
| RAMMODE レジスタ | 48FBh（ミラー 49FBh-4FFBh） |
| RAM モードのバンクレジスタ | 48FCh-48FFh（同上） |
| 互換モードのバンクレジスタ | 5000-57FFh / 7000-77FFh / 9000-97FFh / B000-B7FFh |
| SCC 音源レジスタ | 各 region の 1800-1FFF |
| SCC 窓を開くバンク値 | 3Fh |
| MMIO 窓 | 7FE0-7FFFh（書き込み専用） |

**メモリ構成は決まった**（§3.3 / §9.5）。**256KB を 8KB × 32 バンクに区切り、
`#0`-`#15` が ROM、`#16`-`#31` が RAM。** MSSX のファームウェアがこれを前提に
動くので、RTL にバンク番号の分岐が無いのは WIP だからだと見てよい。

`y8960bas.rom` は 128KB = ROM バンク `#0`-`#15` にちょうど収まる。

窓の初期値は BANK0=`#0`, BANK1=`#1`, BANK2=`#2`, BANK3=`#3`
（**確認済み(読解)**: MSSX の `docs/y8960/hardware.md`。写しの §4 とも一致）。

**MMIO 窓は BANK1 に ROM バンクが出ているときだけ表に出る。**
RAM バンクまたは SCC (`3Fh`) のときは隠れる（§9.5。2026-09-12 にユーザーが確定）。
`rammode` を直接見ない。

#### 実装（Phase 1-1、2026-09-12）

`Src/Memory/romMapperY8960Scc.c`。**256KB を単一の配列で持ち、
`bankReg[region] & 0x1F` でアドレスを決める。** 実機（RTL）が
`bankreg[4:0]` の 5bit しかアドレスに出さないのと同じ形で、
前半 128KB が ROM、後半 128KB が RAM に当たる。

**バンクレジスタは 6bit 保持する。** アドレスに出るのは 5bit だが、
`3Fh` が SCC 窓を選ぶので上位ビットを捨てられない。

**openMSX_Y8960 の規則 4（バンク 3Fh を RAM バンクとして扱わない）は、
こちらでは要らない。** あちらは ROM と RAM を別オブジェクトで持つため
`bank - 16 = 47` が範囲外になったが、単一配列なら `& 0x1F` で
バンク 31 を指すだけで収まる。**RTL の挙動とも一致する。**

| 窓 | アドレス | レジスタ（互換モード） | レジスタ（RAM モード） |
|---|---|---|---|
| BANK0 | 4000-5FFF | 5000-57FF | 4?FCh |
| BANK1 | 6000-7FFF | 7000-77FF | 4?FDh |
| BANK2 | 8000-9FFF | 9000-97FF | 4?FEh |
| BANK3 | A000-BFFF | B000-B7FF | 4?FFh |

RAM モードのレジスタは `4?FBh`（? = 8..F）で、**両モードで常に見える**。

**書き込みの経路は 2 つある。** RAM モードで RAM バンクが出ている region は
`slotMapPage` の `writeEnable` を立てて直接書かせる。**region 0 だけは
`writeEnable` を立てない** — そこにレジスタがあるので、書き込みを
コールバックで受ける必要がある。

#### 確かめたこと（**確認済み** 2026-09-12）

**テスト用のカートリッジ ROM を書いて、MSX 側からレジスタを叩いて確かめた。**
`tests/banktest.asm`。ROM の init から走るので、Y8960 のスロットが
選択された状態で始まり、スロット切り替えが要らない。C-BIOS でも動く。

各バンクの `0800h` にバンク番号を置いた 128KB の ROM を用意し、3 つを見る。

| | 内容 | 結果 |
|---|---|---|
| 1 | 互換モードで BANK1 に ROM バンク 0-15 を順に出し、各々が自分の番号を返すこと | **OK** |
| 2 | RAM モードで RAM バンクに書いた値が残り、**別のバンクには現れない**こと | **OK** |
| 3 | 互換モードでは書き込みが効かないこと | **OK** |

**判別力を確かめてある。** 実装を 2 通りに壊して、期待どおり落ちることを見た。

| 壊し方 | 1 | 2 | 3 |
|---|---|---|---|
| `bankSwitch` が常にバンク 0 を出す | **NG** | **NG** | OK |
| 書き込み許可を常に立てる | **NG** | **NG** | **NG** |
| （戻すと） | OK | OK | OK |

2 つ目の壊し方で 3 つとも落ちるのは、`slotMapPage` に書き込みを許すと
**バンクレジスタへの書き込みもコールバックに来なくなる**ため。
壊し方としては粗いが、3 番目に判別力があることは示せている。

**この検証は `Init ROM in slot:` の表示には一切依存していない。**

**まだ確かめていないこと**: SCC 窓、MMIO 窓、I/O イネーブラ、トンネル。
どれもまだ実装が無い。

#### SCC 音源の接続（Phase 1-2、2026-09-12）

`sccCreate()` は音を `MIXER_CHANNEL_SCC` に固定で載せるので、
**`sccCreateEx(mixer, audioType)` を足して種別を選べるようにした**
（`Src/SoundChips/SCC.{c,h}`）。`sccCreate()` は従来の種別で呼ぶだけの
薄い包みになる。**呼び出し側 8 か所は無変更**で、上流に触るのは 2 ファイルに収まる。
Y8960 は `MIXER_CHANNEL_Y8960` を使う（§6.2）。

**SCC 窓が出ている region は、ページ全体をコールバック経由にする。**
窓は region の 1800-1FFF だけだが、`slotMapPage` は 8KB 単位でしか
読みを許可できないため。窓が閉じている region は従来どおり直接読ませる。

`read` と `peek` は分けてある。**SCC は `Mode::Real` でも読み出しで
deformation レジスタが発火する**ので、デバッガの覗き見（`peek`）が
音源の状態を変えてはいけない。

#### 確かめたこと（**確認済み** 2026-09-12）

`tests/banktest.asm` に 4 番目を足した。BANK1 に `3Fh` を書くと
`7800h` が SCC になり、書いた値が読み戻せる。バンク番号に戻すと
メモリに戻る。**4 つとも OK。**

**判別力を確かめてある。** `sccVisible()` が常に偽を返すようにすると、
**4 番だけが NG になり、1-3 は OK のまま**だった。

#### 検証できない範囲がある

**BANK2 と BANK3 の窓はこのテストでは確かめられない。**
カートリッジの init はページ1（4000-7FFF）だけがカートリッジのスロットに
切り替わった状態で走り、**ページ2（8000-BFFF）は RAM のまま**である。
BANK2 / BANK3 に向けた書き込みはマッパーに届かない。

**一度これで誤った OK を出した。** BANK2 に書いて読み返すテストが通ったが、
届いていたのは RAM で、同じ場所に書いて読んだだけだった
（**確認済み**: マッパーに記録を仕込み、`9000h` への書き込みが
一度も現れないことを見た）。

確かめるには `ENASLT` でページ2 を切り替える必要がある。**未実施。**
ファームウェアが使うのは BANK1 の `7800h` なので、
実害は小さいと見ている（`hardware-notes.md` の SCC 節）。

### 5.8 I/O イネーブラとメモリマップド I/O のトンネル

この 2 つはブロックではなく、**Y8960 SCC のマッパーが持つ機能**である
（窓が 7FE0h-7FFFh にあり、マッパーがメモリ空間を見ているため）。

| 窓 | 内容 |
|---|---|
| 7FF6h | I/O Enabler 1。b0 = OPLL#1 (7Ch-7Dh)、b1 = OPLL#2 (7Ah-7Bh) |
| 7FFFh | I/O Enabler 2。b0/b1 = OPL2、b2/b3 = DCSG、b4 = SSG、b7 = MSX-TIMER |
| 7FEAh-7FF5h | 各ブロックへのトンネル。**イネーブラの影響を受けない** |

**窓そのものが出る条件は BANK1 の中身で決まる**（§9.5）。BANK1 に ROM バンクが
出ているときだけ表に出て、RAM バンクまたは SCC のときは**イネーブラごと隠れる**。
つまり **RAM バンクを BANK1 に出したままではイネーブラを書き換えられない。**

**リセット直後は全部閉じている**（openMSX 側の判断を踏襲。RTL は 7Ah 側と
A0h 側をリセット時有効にしているが、これは WIP のプレースホルダとして採らない）。

窓の中は**高位側が 1 番目**。直接 I/O 側とは並びが違う（`hardware-notes.md` §3）。
**片方から他方を推論してはならない。**

**イネーブラの実現方法は §4.2 を直したことで素直になった。**
ポートは重ねて登録できるので、Y8960 は自分の登録を常に持ったままにし、
**閉じているときは `writeIO` の中で書き込みを捨てる**だけでよい
（openMSX_Y8960 も同じ形にしている）。本体側が先に取っていても構わない。

`ioPortUnregister` でポートを出し入れする必要は無い。読み出しのある
ブロック（OPL2 と MSX-TIMER）は、閉じている間 `readIO` が `0xFF` を返せば
AND 合成で他のデバイスの邪魔をしない。

#### 実装（Phase 1-3、2026-09-12）

窓は `7FE0h`-`7FFFh`、**書き込み専用**。`RomMapperY8960Scc` が持つ。

**窓が出る条件は BANK1 の中身だけで決まる**（§9.5）。ROM バンクなら出て、
RAM バンクまたは SCC なら消える。SCC 窓 `7800h`-`7FFFh` は MMIO 窓を
完全に含むので、両者は部分的に重ならず、条件が 1 本の式で書ける。

| アドレス | 内容 |
|---|---|
| `7FF6h` | I/O Enabler 1 |
| `7FFFh` | I/O Enabler 2 |
| `7FEAh`-`7FF5h` | 各ブロックへのトンネル（Phase 1-4） |

**イネーブラを他のブロックから読む口を先に作った。**
`romMapperY8960.h` の `y8960IoEnabled(Y8960IoBlock)`。
窓は SCC ブロックの持ち物なので、実体はそこにあり、
**カートリッジが 1 枚である前提でシングルトンにしている**（§9.3 と同じ判断）。

**SCC ブロックが構成に無ければ 0 を返す。** ブロックを単独でマシン構成に
置いたとき、イネーブラが無いからといって開きっぱなしにはしない。

**ミラーの `3FE0h`-`3FFFh` は実装していない。** 実機は `A14` を見ないので
両方に出るが、カートリッジに割り当たるのは 4000-BFFF の 4 ページで、
ページ0 は本体のものである。CPU のアクセスがマッパーに届かない。

#### 確かめられないこと

**窓が出ていることは、外から見て確かめられない。**

| BANK1 | 窓 | `7FF6h` への書き込み | 読み返し |
|---|---|---|---|
| RAM バンク | 無い | ページが書けるので直接 RAM へ（マッパーに届かない） | 書いた値 |
| ROM バンク | 有る | イネーブラへ | ROM の値 |

**どちらの行も、窓の有無とは別の理由だけで説明が付く。**
RAM の側は「書ける RAM だから」、ROM の側は「ROM だから」で足りる。

**判別力の無いテストを 1 つ書いて、消した。** RAM バンクのとき `7FF6h` が
RAM として読み書きできることを見るテストを足したが、
`mmioVisible()` が常に真を返すように壊しても**通ったまま**だった。
書き込みがページ経由で直接 RAM に行き、窓の判定に届いていない。
テスト 2 の焼き直しでしかなかったので削除した。

**窓が出ていること自体は、マッパーの側に記録を置けば確かめられる**
（Phase 1-4 でそうした）。**MSX 側から区別できないだけである。**

**イネーブラが効いていることは、Phase 1-5 の MSX-TIMER で確かめられた**（§5.5）。
タイマーが最初の「反応するブロック」になった。`7FFFh` への書き込みが
この窓を通ってイネーブラに届き、タイマーのポートを開けている。

#### トンネル（Phase 1-4、2026-09-12）

窓の `7FEAh`-`7FF5h` が各ブロックへの書き込み口になる。

**イネーブラの影響を受けない。** イネーブラ自身がこの窓の中にしか無いので、
トンネルまで閉じると開く手段が無くなる。

**ブロックは自分で名乗り出る。** `y8960RegisterTunnel()` で登録し、
破棄するときに `y8960UnregisterTunnel()` で外す。
**外せるのは登録した本人だけ**にしてある（`ref` を照合する）。
差し替えで新しいデバイスが登録した後に古い方が破棄されても、
新しい側の登録を剥がさない。誰も登録していないトンネルへの書き込みは捨てる。

#### 確かめたこと（**確認済み** 2026-09-12）

`tunnelWrite()` に記録を入れ、テスト ROM から `7FEAh` から 12 バイトに
`A0h` から順の値を書いた（`tests/banktest.asm` の `tunnels:`）。
**12 件すべてが表どおりの宛先に届いた。**

| アドレス | 宛先 | port |
|---|---|---|
| 7FEA / 7FEB | SSGS | 0 / 1 |
| 7FEC / 7FED | **OPL2 #1** | 0 / 1 |
| 7FEE / 7FEF | OPL2 #0 | 0 / 1 |
| 7FF0 / 7FF1 | DCSG #1 / #0 | 0 |
| 7FF2 / 7FF3 | OPLL #1 | 0 / 1 |
| 7FF4 / 7FF5 | OPLL #0 | 0 / 1 |

**窓が出ていることも、これで同時に確かめられた。** 窓が閉じていれば
`tunnelWrite()` は呼ばれない。**MSX 側からは区別できないだけで、
マッパーの側に記録を置けば見える**（§5.8 の「確かめられないこと」を
そう読み替えること）。

記録はコミットしていない。テスト ROM の掃き出しは残してあるので、
同じ確認をするときは `tunnelWrite()` に数行足せばよい。

**受け手はまだ無い。** 7 ブロックのどれも `y8960RegisterTunnel()` を
呼んでいないので、届いた先で何も起きない。**音が出るかどうかは Phase 2。**

## 6. 外に出る値

**一度決めると、変えたときに外へ波及する値**をここに集める。
二重に持つと片方が古くなるので、値はこの節にだけ書く。

### 6.1 RomType — `config.ini` に書かれる数値

`Machines/<名前>/config.ini` の `[Slots]` 行に**数値で**書かれる。
既存の最大は `ROM_MAXROMID = 179`（**確認済み(読解)**: `Src/Media/MediaDb.h`）。

**2026-09-12 に確定**（ユーザー判断）。並びはユーザーが挙げたブロックの順。

| 値 | enum | 短縮名 | 表示名 | ブロック |
|---|---|---|---|---|
| 180 | `ROM_Y8960OPLLEX` | `Y8960OPLLEX` | Y8960 OPLLEX | OPLLEX |
| 181 | `ROM_Y8960OPL2EX` | `Y8960OPL2EX` | Y8960 OPL2EX | OPL2EX |
| 182 | `ROM_Y8960SSGS` | `Y8960SSGS` | Y8960 SSGS | SSGS |
| 183 | `ROM_Y8960TIMER` | `Y8960TIMER` | Y8960 MSX-TIMER | MSX-TIMER |
| 184 | `ROM_Y8960MIXER` | `Y8960MIXER` | Y8960 MSX-MIXER | MSX-MIXER |
| 185 | `ROM_Y8960SCC` | `Y8960SCC` | Y8960 SCC | SCC + マッパー + イネーブラ + 窓 |
| 186 | `ROM_Y8960DCSG` | `Y8960DCSG` | Y8960 DCSG | DCSG ×2 |

`ROM_MAXROMID` は **186** になる。

**短縮名はコマンドラインにも出る。** `romTypeToShortString` の戻り値は
`/romtype1` / `/romtype2` が受け取る名前と照合され、`/listromtypes` で一覧される
（**確認済み(読解)**: `Src/Emulator/CommandLine.c:53-65` の
`romTypeFromShortString`）。テーマのトリガーでも使われる。

**やり直しの値段**: 数値を変えると、既に作った `config.ini` が全部ずれる。
短縮名を変えると、それを書いたコマンドラインとスクリプトが通らなくなる。
**表示名だけは言語リソースの中なので安く変えられる。**

### 6.2 ミキサーのチャンネル種別

**2026-09-12 に確定**（ユーザー判断）: **Y8960 専用の種別を 1 つ新設し、
Y8960 の全ブロックをそこに載せる。**

```
MIXER_CHANNEL_Y8960
```

`Src/SoundChips/AudioMixer.h` の `MixerAudioType` に足す。
**置く位置は `MIXER_CHANNEL_CASSETTE` の後、`MIXER_CHANNEL_TYPE_COUNT` の前。**
同ファイルのコメントが「`MIXER_CHANNEL_MIDI` より前に挿すな」と書いており、
実際 `mixerRecalculateType` が `mixer->channels + MIXER_CHANNEL_MIDI` と
**型の値で実チャンネル配列を索引している**（**確認済み(読解)**: `AudioMixer.c:163-164`）。
MIDI より後ろに足す限りこの索引は動かない。

**この識別子名がそのまま設定ファイルのキーになる。**
`Properties.c` のマクロが enum 定数名を文字列化するため
（**確認済み(読解)**: 同ファイル 592 行の `GET_INT_VALUE_2s1` が `#v3` を使う）、
`bluemsx.ini` に次の 3 行が増える。

```
sound.mixerChannel.MIXER_CHANNEL_Y8960.enable
sound.mixerChannel.MIXER_CHANNEL_Y8960.pan
sound.mixerChannel.MIXER_CHANNEL_Y8960.volume
```

**やり直しの値段**: 識別子を変えると、利用者の `bluemsx.ini` に書かれた
Y8960 の音量・パン・有効の設定が既定値に戻る（キーが読めなくなるだけで、
壊れはしない）。

**未決**: 音量スライダを UI のどこに出すか、テーマ側に何が要るかは
調べていない（§9.6）。

### 6.3 セーブステートの節名

`deviceManagerRegister` に渡した `RomType` と、`saveStateOpenForWrite("名前")` の
名前でセーブステートの節が決まる（**確認済み(読解)**:
`romMapperTurboRTimer.c:66, 119`）。

| ブロック | 節名 |
|---|---|
| OPLLEX | `y8960OpllEx` |
| OPL2EX | `y8960Opl2Ex` |
| SSGS | `y8960Ssgs` |
| MSX-TIMER | `y8960Timer` |
| MSX-MIXER | `y8960Mixer` |
| SCC | `y8960Scc` |
| DCSG | `y8960Dcsg` |

**回路が 2 つあるブロック（OPLLEX / OPL2EX / DCSG）は、1 デバイスが 2 回路を
まとめて持つか、2 デバイスを別々に置くかで節の数が変わる。** §9.7。

### 6.4 まだ確定していない外部値

| 値 | 状態 |
|---|---|
| MSX-MIXER の I/O アドレス B6h-B7h | **暫定**。hra1129 さんが実装した時点で変わりうる（`hardware-notes.md` §7.4） |
| ROM DB の XML 名（`mediaDbStringToType` が受ける名前） | ROM ファイルを伴うのは Y8960 SCC だけ。必要になってから決める |
| 出力切り替えの設定名 | §9.4 が決まってから |

## 7. フェーズ計画

**Phase 0 の終わりに、実際に走る器ができていること**を目標にする。
中身が空でも、マシン構成に 7 ブロックを置いて起動できる状態にする。
そうすれば以降の実装は「中身を入れる」だけになり、後出しの仕様変更を
構造が吸収できる。

### Phase 0 — 器を作る【完了 2026-09-12】

- 本書と `doc/fork/README.md`（済み）
- **ビルド環境**（済み）。手順とこのマシンの事情は `doc/fork/build/README.md`
- §9.1（I/O ポートの重ね合わせ）の決着（済み。§4.2）
- **`RomType` 7 枠**（済み。§6.1 の確定値をそのまま）
- **ミキサー種別 `MIXER_CHANNEL_Y8960`**（済み。§6.2）
- **ビルド定義 3 系統への追記**（済み）
- **マシン構成エディタへの登録**（済み）
- **7 ブロックを置いた構成で起動できることを確認**（済み）

**触ったファイル**。

| ファイル | 内容 |
|---|---|
| `Src/Media/MediaDb.h` | enum 180-186、`ROM_MAXROMID` を 186 へ |
| `Src/Media/MediaDb.cpp` | 名前 3 種（DB 名 / 表示名 / 短縮名）と `romTypeIsMegaRom`（SCC のみ） |
| `Src/Memory/romMapperY8960.h` | 7 つの `Create` の宣言 |
| `Src/Memory/romMapperY8960*.c` | **7 ファイル**。今は `deviceManagerRegister` するだけ |
| `Src/Board/Machine.c` | 7 ブロックの生成 |
| `Src/Emulator/RomTypeList.c` | **Y8960 SCC だけ** `mapperList` へ（ROM を伴うため） |
| `Src/SoundChips/AudioMixer.h` | `MIXER_CHANNEL_Y8960` |
| `Src/Emulator/Properties.c` | 同種別の既定値と設定の読み書き |
| `Src/Win32/Win32machineConfig.c` | 一覧と、ROM ファイルの要否の判定 6 か所 |
| `Make/msvc2022`, `Make/msvc2026`, `Make/gcc` | 新規 8 ファイルの登録 |

**Y8960 SCC だけは「ROM を伴う側」に分類した。** 構成エディタでは
ROM ファイルを指定でき、`romTypeIsMegaRom` でも真を返す。
Phase 1 で ROM を読むようになったときに分類を変えずに済む。
**ただし `Machine.c` では今も ROM 無しデバイスとして生成している**ので、
Phase 1 で後半の `switch` へ移す（`Create` の引数も変わる）。

#### 確かめたこと（**確認済み** 2026-09-12）

| | |
|---|---|
| ビルド | Release / x64 が 0 エラー。警告はすべて既存 |
| `/listromtypes` | `Y8960SCC  Y8960 SCC` が出る |
| `/listmachines` | テスト構成が出る（96 → 97 機種） |
| **起動** | **C-BIOS 0.29+ の起動画面が出た。** `Machine.c` は `success &=` で集約するので、**7 ブロックすべての生成に成功している** |

**テスト構成**は `doc/fork/y8960/tests/MSX2+ - C-BIOS + Y8960/config.ini`。
C-BIOS MSX2+ に 7 行足しただけで、ROM は C-BIOS のものを相対パスで参照する。
使うときは出力先の `Machines/` にコピーする。

#### まだ無いもの

**中身。** 7 つのデバイスはどれも `deviceManagerRegister` するだけで、
I/O ポートも取らず、音も出さない。イネーブラも窓もマッパーも無い。
**それらは Phase 1 以降。**

### Phase 1 — 音の出ないブロックから【完了 2026-09-12】

イネーブラと窓の枠組みが要るので、**SCC とマッパーを先に作る**。
これが無いと他のブロックを開けられない。

**もう一つ理由がある。** マッパーが動けば `y8960bas.rom` を載せられるので、
以降のブロックは**ファームウェアを走らせて確かめられる**ようになる（§8.3）。

- **バンクメモリとバンクレジスタ**（§5.7）【完了 2026-09-12】
- **SCC 音源の接続**（§5.7）【完了 2026-09-12】
- **MMIO 窓と I/O イネーブラ**（§5.8）【完了 2026-09-12】
- **各ブロックへのトンネル**（§5.8）【完了 2026-09-12】
- **MSX-TIMER**（§5.5）【完了 2026-09-12】

### Phase 2 — 音源

- **DCSG**（§5.4）【完了 2026-09-12】
- OPLLEX（§5.2）
- OPL2EX（§5.1）
- SSGS（§5.3）— §9.1 の結論に依存

### Phase 3 — 仕上げ

- MSX-MIXER（§5.6）
- openMSX_Y8960 との突き合わせ
- ユーザー向け文書

## 8. 検証方法

**ここが openMSX 側と最も違う。** ただし、**当初書いた「blueMSX+ には
自動テストの手段が無い」は言いすぎだった**。2026-09-12 に実測して分かった
ヘッドレスの経路を以下に含める。

openMSX_Y8960 は Tcl スクリプトで自動回帰テストを 13 本持っており、
どれも「期待値を外した構成で落ちる」ことまで確かめてある。
**blueMSX+ に同じことはできない** — エミュレータを走らせながら
レジスタを叩くスクリプト機構が無いため（**確認済み(読解)**:
`Src/Emulator/CommandLine.c:330-383` のオプション表に、スクリプトを実行する
手段も、音を録り始める手段も無い。`/debugger` はデバッガのウィンドウを
開くだけである）。

### 8.1 ヘッドレスで確かめられること

`/listromtypes` `/listmachines` `/listspecials` `/listthemes` は、一覧を
標準出力に書いて `exit(0)` する。**エミュレータのウィンドウが作られる前**に
終わる（**確認済み(読解)**: `Src/Win32/Win32.c` の `commandLinePrintLists` と
4892 行の `exit(0)`。**確認済み**: 実行してもプロセスもウィンドウも残らず、
終了コード 0 と出力が返った）。

| 手段 | Y8960 で何に使えるか |
|---|---|
| `/listromtypes` | **§6.1 の RomType 7 枠が登録されたか。**表示名と短縮名もそのまま出る |
| `/listmachines` | **7 ブロックを置いたマシン構成が読めるか。** `config.ini` をパースし、ROM ファイルの実在まで見る |

**`/machinedir` で作業ツリーの `Machines` を直接読ませられる**ので、
テスト用構成をコピーせずに試せる（**確認済み**）。

**Phase 0 の「器ができた」は、この 2 つで判定できる。**

### 8.2 ヘッドレスでは確かめられないこと

**デバイスの生成は走らない。** `machineIsValid` は `config.ini` のパースと
ROM の実在までで、`machineInitialize()`（`romMapper*Create` を呼ぶところ）には
届かない（**確認済み(読解)**: `Src/Board/Machine.c:647-690`）。

したがって次は**エミュレータを実際に起動しないと分からない**。

- **I/O ポートの登録が通ったか**（§4.2 の先勝ちで黙って落ちていないか）。
  これは Y8960 で最も危ないところなので、**起動しての確認が要る**
- 音が出るか、レジスタの挙動

| 手段 | 何ができるか | 未確認な点 |
|---|---|---|
| GUI で手動操作 | すべて。ただし人の手が要る | — |
| 音声ログ | `mixerStartLog()` が WAV を書く（**確認済み(読解)**: `AudioMixer.c:630`、UI からは `Actions.c:406`） | コマンドラインから開始する手段が見当たらない |
| MSX 側のテストプログラム | レジスタを叩いて結果を見る。BASIC 拡張やアセンブリで書く | 結果をホスト側へ取り出す経路 |
| デバッガプラグイン | メモリと I/O を覗く | 自動化できるか |

**起動するとウィンドウが出る。走らせる前に一声かけること。**

**非回帰テストの注意**（openMSX 側と同じ）: 修正後に通ることは、
**修正前に落ちることを示すまで証拠にならない。**
テストを書いたら、まず期待値を壊して落ちるのを確認する。

**実機が無いので「実機と一致」は検証できない。** できるのは
「openMSX_Y8960 と同じ挙動になること」までである。
openMSX 側は実測済みなので、**同じレジスタ列を与えて音を比べる**のが
最も確度の高い突き合わせになる。

### 8.3 最も強い検証 — ファームウェアを走らせる

**Y8960 を駆動する BIOS と拡張BASIC が実在する**（§3.3）。
madscient/MsxSoundSuiteExtension の `y8960bas.rom`、**128KB**。

**これは openMSX_Y8960 が持っていなかったものである。** あちらの
`hardware-notes.md` §8 は「BIOS ROM イメージが存在しない」と書いている
（hra1129/Y8960_Cartridge には無い、という意味では今も正しい）。

Y8960 SCC のマッパー（§5.7）が動けば、**この ROM を載せて起動できる**。
128KB は ROM バンク `#0`-`#15` にちょうど収まる。

**ファームウェアが起動して音が出れば、それは次のすべてが同時に通ったことを
意味する。**

- バンクレジスタと窓のデコード（§5.7）
- メモリマップド I/O のトンネル（§5.8）
- I/O イネーブラ（同上）
- 叩かれた音源ブロックそのもの

**どの単体テストより射程が広い。** 逆に、落ちたときにどこが悪いかは
分からないので、単体の確認と併用する。

**ただし起動が要るので、ウィンドウが出る**（§8.2）。自動で繰り返せない。

**ROM はリポジトリに入れない**（§3.1）。実機の BIOS ROM と同じく、
gitignore 済みのビルド出力に置く（`doc/fork/build/README.md` §4）。

**前提**: MSSX 側も開発中である。Y8960 BASIC Extension と
Y8960 Sequencer BIOS は「（開発中）」と明記されている（**確認済み(読解)**:
MSSX の `README.md`）。**動かないときに、こちらが悪いとは限らない。**

## 9. 未決事項

**着手の順に並べてある。** 9.1 と 9.5 は決着した。

### 9.1 I/O ポートの重ね合わせ — **決着済み。上流を直した（2026-09-12）**

**ユーザー判断**: 「I/O ポートの多重化は上流を改造するしか無いのでそうしてください。
（というかエミュレータとしては普通に不具合だと思います）」

**実装済み。** 内容と、ついでに見つかった既存の不具合 3 件は §4.2。

当初ここに 3 案（A: `IoPort.c` を重ね合わせ対応 / B: Y8960 が本体 PSG へ中継 /
C: 本体内蔵版だけ作る）を並べて A を推していた。**A を採った。**

**着手前の前提として「現状どこかで登録が黙って落ちていないか」を挙げていたが、
確かめる前に着手した。** 静的には分からず（構成しだい）、確かめるには
検出コードを入れて各機種を起動することになる。**結局その起動は、
直した後の非回帰確認として必要**なので、順序を入れ替えて後で一度に見る。
**未確認**のまま残っている。

**残るのは起動しての確認だけ**（§4.2 の末尾、§9.8）。

### 9.2 OPLL-X (YM2423) の音色データをどこから取るか

§5.2。候補は `NukedOPLL/opll.c` の `patch_ym2423`（GPLv2、同リポジトリ内）か、
openMSX_Y8960 経由の "Copyright free OPLL(x) ROM patches"（CC BY-SA）。

**急がない。** 暫定で OPLL と同じ表を入れておき、後で差し替えられる（§5.2）。

### 9.3 MSX-MIXER のゲインを各ブロックへどう渡すか

§5.6。シングルトンか、`deviceManager` から引くか。
**Y8960 を 2 台挿す構成があり得るかで答えが変わる。** 実機が 1 台しか無い以上、
当面はシングルトンで足りる（**推測** — 根拠は Y8960 がカートリッジ 1 枚であること。
複数挿しの可否を hra1129 さんに確認してはいない）。

### 9.4 出力の切り替えをどう出すか

openMSX_Y8960 には「Y8960 の音だけ / 本体の音だけ / 両方」を選ぶ設定がある。
**カートリッジは音声を MSX 本体に戻さない**という実機の性質を映したもの
（`hardware-notes.md` には無く、openMSX_Y8960 の implementation-plan §3.7 にある。
出所は hra1129 さんの回答）。

blueMSX+ で同じものを出すなら、設定項目と UI が要る。
**Y8960 専用のミキサー種別を作った（§6.2）ので、「Y8960 だけ鳴らす」は
既存の種別ごとのミュートで概ね代用できる**（**推測** — 種別ごとの enable が
あることは確認済みだが、代用になるかを試してはいない）。専用設定が要るかは
Phase 3 で決める。

### 9.5 SCC バンクメモリ — **決着済み（2026-09-12）**

§5.7 / §3.3。MSSX の記述と、**2026-09-12 のユーザーからの直接の回答**で
すべて決まった。**未決は残っていない。**

| | 決まったこと |
|---|---|
| ROM / RAM の境界 | **256KB、`#0`-`#15` が ROM、`#16`-`#31` が RAM。** ファームウェアがこれを前提に動く |
| SCC 窓 | **BANK1 に `3Fh` を書くと `7800h` に出る。** ファームウェアはここを使う |
| **MMIO 窓の出現条件** | **BANK1 に ROM バンクが出ているときだけ表に出る。RAM バンクまたは SCC のときは隠れる** |

**ROM 128KB + RAM 128KB の分割が正しい。** RTL にバンク番号による分岐が
無いのは、RTL が WIP だからだと見てよい（§2 の前提と整合する）。
**openMSX_Y8960 の既存実装（分割して持つ）が実機の意図に合っていた**ことになる。

blueMSX+ は**この境界で新規に書く**。openMSX 側が保留にしていた #2/#3/#5 は、
blueMSX+ では迷う必要が無い。

#### MMIO 窓の条件は BANK1 の中身だけで決まる

**2026-09-12 にユーザーから直接の回答があった**（最も強い出典）。

> BANK1 の MMIO は ROM が選択されている場合のみ表に出る。
> RAM または SCC が出ている時は MMIO は隠れる。

**`rammode` を直接見ない。** 見るのは BANK1 のバンクレジスタが何を指しているかだけ。

| BANK1 の中身 | MMIO 窓 |
|---|---|
| ROM バンク（`#0`-`#15`） | **出る** |
| RAM バンク（`#16`-`#31`） | 隠れる |
| SCC（`3Fh`） | 隠れる |

**これは openMSX_Y8960 の規則 2 と一致する**（あちらの §3.8.1 が
`!(ramEnabled && isRamRegion(3)) && bankreg1 != 3Fh` と書いている）。
**両者の挙動は割れない。**

**MSSX の文書を「RAM モード 0 のとき**または**BANK1 に ROM」と読んだのは
誤りだった**（本書が 2026-09-12 に一度そう書いた）。RAM モード 0 であっても、
BANK1 が RAM バンクや SCC を指していれば MMIO は隠れる。

**前提**: MMIO 窓のアドレスが `7FE0h`-`7FFFh` から動かないこと。
動けば SCC 窓 `7800h`-`7FFFh` との包含関係が崩れ、条件が書き直しになる。

### 9.6 ミキサーの音量スライダをどこに出すか

§6.2。UI とテーマへの影響を調べていない。

### 9.7 2 回路を 1 デバイスにするか 2 デバイスにするか

OPLLEX / OPL2EX / DCSG は 2 回路ある。

- **1 デバイスが 2 回路を持つ**と、マシン構成に 1 行置けば 2 回路そろう。
  片方だけ載せる構成が作れない
- **2 デバイスを別々に置く**と、1 回路だけの構成が作れる。
  `config.ini` に 2 行書くことになり、`RomType` も 2 つ要る（§6.1 の表が変わる）

**「個別にマシン構成に追加できるようにする」という指示は、
ブロック単位を指していると読んだ**（ユーザーが挙げた一覧が回路単位ではなく
ブロック単位だったため）。§6.1 は 1 ブロック 1 `RomType` で組んである。
**回路単位まで分けたいなら、`RomType` を決める前に言ってほしい**（§6.1 の
値段はここが一番高い）。

### 9.8 起動を伴う検証をどう回すか

§8.2。ヘッドレスで届く範囲は 2026-09-12 に分かった（§8.1）。
**残るのは「起動しないと分からないこと」をどう繰り返し確かめるか**である。
とくに §4.2 / §9.1 の I/O ポートの重なりは、起動しないと踏めない。

候補は §8.2 の表。**未決。**

## 10. 見送った提案

- **ymfm を導入して Y8960emu のコアをそのまま移植する** — 却下（2026-09-12、
  ユーザー判断）。blueMSX+ に無い依存を持ち込むことになる。
  OPLLEX は emu2413 のフォークで作る
- **OPLLEX を複数バックエンドで作る** — 却下（2026-09-12、ユーザー判断）。
  emu2413 のフォーク 1 本にする。既存 MSX-MUSIC のようなバックエンド切替は
  Y8960 には持ち込まない
- **ブロックごとにミキサー種別を新設する** — 却下（2026-09-12、ユーザー判断）。
  音量スライダが 5 本増え、UI と設定ファイルへの影響が大きい。
  Y8960 専用の種別 1 つにまとめる
- **ブロックごとに既存のミキサー種別へ相乗りする** — 却下（同上）。
  本体側の同種の音と音量が連動してしまい、分離できない

## 11. 実行経緯

### 2026-09-12 (9) — Phase 0 完了。器ができた

**7 ブロックが `RomType` を持ち、マシン構成に置いて起動できるようになった**（§5 Phase 0）。
中身はどれも空で、I/O ポートも取らず音も出ない。

**確かめたこと**: ビルド 0 エラー、`/listromtypes` に `Y8960SCC` が出る、
テスト構成が `/listmachines` に出る（96 → 97）、そして
**C-BIOS 0.29+ の起動画面が出た**。`Machine.c` が `success &=` で集約するので、
**起動できたこと自体が 7 ブロックすべての生成に成功した証拠**である。

**触った上流ファイルは 10 本**（表は §5 Phase 0）。`doc/fork/README.md` の
「上流のファイルに触ったら記録する」にも効いてくるので、そちらの一覧も更新した。

#### 決めたこと

- **`RomType` は §6.1 の確定値をそのまま使った**（180-186）。
  文書に先に書いておいたので、実装時に迷いが無かった
- **Y8960 SCC だけ「ROM を伴う側」に分類した。** 構成エディタで ROM ファイルを
  指定でき、`romTypeIsMegaRom` も真を返す。Phase 1 で ROM を読むときに
  分類を変えずに済む。**ただし `Machine.c` では今も ROM 無しとして生成している**
- **新しい `RomType` に言語リソースは要らない。** `romTypeToString` は
  `ROM_FLASHROMSCC` 以降、言語関数ではなく直接文字列を返す形になっている
  （**確認済み(読解)**）。14 言語のファイルを触らずに済んだ
- **ファイルは最初から 7 本に分けた。** 後で分けるとビルド定義 3 系統を
  触り直すことになる

#### つまずき

**heredoc の中でバックスラッシュが 1 段落ちる。** `vcxproj` のパス
（`..\..\Src\Memory\...`）を書こうとして `\r` が改行に化け、
アンカーが一致しなかった。`chr(92)` で組み立てて回避した。
**この作業ツリーでは、バックスラッシュを含む文字列をヒアドキュメントに
直接書かないこと。**

### 2026-09-12 (8) — 配りが効いていることを実測した

**7Ch / 7Dh への書き込みが両方のデバイスに届くことを数で確かめた**（§4.2）。
本体 MSX-MUSIC と FM-PAC の書き込み口それぞれに記録コードを入れ、
`MSX2+ - Sony HB-F1XDJ` + FM-PAC で `CALL MUSIC` から演奏した。
**258 件ずつ、完全に同数**だった。記録コードはコミットしていない。

**FM-PAC は 258 件すべてを `enable=00` で捨てていた。** 仕様どおりで、
鳴っていたのは本体 MSX-MUSIC だけである。**「届いているが自分で捨てる」**という
この形は、**Y8960 の I/O イネーブラに採ろうとしている設計そのもの**（§5.8）。
既存デバイスが同じ作りで動いていることの裏付けになった。

#### 測り方で 2 度つまずいた

どちらも**測定器が動いていることを先に確かめなかった**ことによる。

1. **記録コードを「重なったときだけ書く」形で入れ、0 件という結果を得た。**
   重なりが無いのか、記録コードが動いていないのかを区別できない。
   全件を書く形に変えて初めて、動いていることと件数の両方が分かった
2. **ビルドが走っていないことに気づかず、古い実行ファイルで測った。**
   `Remove-Item` がツールに拒否されてスクリプトが途中で止まっており、
   ビルドログが作られていなかった。**実行ファイルの中に記録コードの文字列が
   あるかを見て**、ようやく分かった

**「0 件」は、測定器に判別力があることを示すまで何の証拠でもない。**
ルートの規則に書いてあることを、2 度とも踏んだ。

#### キー入力は自動化できない

`SendKeys` で `CALL MUSIC` を送っても MSX には届かない（**確認済み**:
BASIC の画面に文字が出ないことを写して見た）。DirectInput 経由と見られる
（**未確認** — 入力のコードは読んでいない）。
**MSX 側の操作が要る検証は、人の手を借りるしかない。**

### 2026-09-12 (7) — 起動しての非回帰確認と、無関係な症状に費やした回り道

**やったこと**: §4.2 の変更を入れた実行ファイルで 4 機種を起動し、
登録の重なりを実測した（表は §4.2）。**FM-PAC を内蔵 MSX-MUSIC 機に挿すと
7Ch / 7Dh が 2 件になる**ことを見た。変更前はここで FM-PAC 側が
黙って落ちていたことになる。

記録コードに判別力があることも確かめた（重なりの無い構成では 0 件）。
記録コードはコミットしていない。

#### 回り道を 1 つ記録する

**「MSX の画面が出ない」という症状を、自分の変更のせいだと疑って調べた。
無関係だった。** 原因は初回起動時の既定ドライバ `video.driver=d3d12` で、
この環境では描画が進まない（`doc/fork/build/README.md` §3.3）。

**判断を誤った箇所が 2 つある。**

1. **自分で撮ったスクリーンショットに症状が写っていたのに、読み違えた。**
   MSX の画面が白いのを「最前面でないので背後のウィンドウが写っている」と
   解釈した。**実際には MSX が描画されていなかった。**
   ウィンドウの中身なのか背後なのかを確かめずに、都合のよい方に解釈した
2. **切り分けを後回しにした。** 変更前のビルドと比べれば一度で分かることを、
   コードを読み直す方から始めた。**比較できる状態を先に作るべきだった**

**ビルド成果物を上書きしたことも混乱の一因だった。** 上流向けの PR ブランチ
（develop ベース）でビルドしたとき、同じ出力先の実行ファイルを置き換えた。
利用者が起動したのはそちらで、こちらの変更は入っていなかった。
**別ブランチでビルドするときは、出力先が共有であることを意識する。**

### 2026-09-12 (6) — I/O ポートの重ね合わせを上流で直した

**ユーザー判断**: 「I/O ポートの多重化は上流を改造するしか無いのでそうしてください。
（というかエミュレータとしては普通に不具合だと思います）」

**§9.1 が決着し、Phase 0 の入口を塞いでいたものが無くなった。**
変更の内容は §4.2。

**やったこと**。

- `Src/Memory/IoPort.{c,h}` を 1 ポート 4 件まで登録できるようにした。
  書き込みは全件に配り、読み出しは駆動するものの AND を取る
- `ioPortUnregister(port, ref)` に `ref` を足し、**53 ファイル 200 箇所**を直した。
  194 箇所は機械的に決まった（同じファイル内で同じポートを登録している行の
  第 4 引数）。残り 6 箇所は手で見た
- **ビルドが通ることを確認**（**確認済み**: 0 エラー）

**ついでに既存の不具合が 3 件見つかった**（§4.2 に表）。
`romMapperKanji12` が他人のポートを解放していた件、
`romMapperOpcodeModule` が登録した側を解放していなかった件、
`Sf7000PPI` の解放漏れ。**どれも「1 ポート 1 デバイス」の下では実害が出にくく、
重ね合わせを許すと実害になる。**

**解放漏れを機械で洗った。** 登録したポートと解放したポートの集合を
ファイルごとに突き合わせる使い捨てのスクリプトを書いた。
検出は 3 ファイル、うち 2 つ（`VDP.c`、`MSXMidi.c`）は
ループ範囲を文字列一致で見た誤検出で、本物は `Sf7000PPI.c` だけだった。

**踏んだ手順の省略を 1 つ記録する。** §9.1 には着手前の前提として
「現状どこかで登録が黙って落ちていないか確かめる」と書いていたが、
**確かめずに着手した。** 静的には判定できず、確かめるには検出コードを入れて
各機種を起動することになる。その起動は直した後の非回帰確認として
どのみち必要なので、順序を入れ替えた。**未確認のまま残っている。**

**この時点では未検証だった。** 起動しての確認は同日中に行った（次の項）。

### 2026-09-12 (5) — ADPCM の RAM は 4 状態をとる

**2026-09-12 にユーザーから直接の回答があった。**

256KB を 2 回路でどう分けるかに **4 状態**ある（共有 / 片寄せ ×2 / 分割 128+128）。
**設定の方法は実機側で未実装**で、**暫定として起動時は共有モード**と置く。
ハードウェア側で決まり次第、修正する。MSSX も共有モードで動く前提。

写しの §7.5 が「manual にあるがレジスタが無い」と書いていたのは、
この状態を指していたことになる。**エミュレータ側で先に決めても、
突き合わせる相手がまだ無い。**

**器は 4 状態ぶん作る**（§5.1）。実装するのは共有モードだけだが、
サンプル RAM を回路の外に 1 つ持ち、各回路が見る範囲をモードで決める形に
しておく。回路ごとに RAM を持たせると、切り替えが決まったときに作り直しになる。

**openMSX_Y8960 はどの状態にも当たっていない。** `<sampleram>256` を
ブロックごとに持たせているので、2 回路が別々の 256KB を見ている。


### 2026-09-12 (4) — MMIO 窓の条件が確定し、誤読が 1 件見つかった

**2026-09-12 にユーザーから直接の回答があった。**

> BANK1 の MMIO は ROM が選択されている場合のみ表に出る。
> RAM または SCC が出ている時は MMIO は隠れる。

**§9.5 に残っていた最後の 1 点が解けた。SCC バンクメモリに未決は無い。**
見るのは BANK1 のバンクレジスタが何を指しているかだけで、`rammode` は
直接には効かない。

**訂正**: 本書は同日、MSSX の「RAM モード 0 のとき、または BANK1 に ROM
バンクが出ているときにしか現れない」という文を**2 条件の or と読み**、
RAM モード 0 で BANK1 が SCC のときに両者が食い違うと書いた。**誤読だった。**
条件は 1 つで、BANK1 の中身だけで決まる。

**結果として openMSX_Y8960 の規則 2 と完全に一致した**
（`!(ramEnabled && isRamRegion(3)) && bankreg1 != 3Fh`）。
**両者の挙動は割れない。** あちらが「RTL の `~rammode` の置き換え（判断）」
として書いた条件は、判断ではなく仕様どおりだったことになる。

**副次的に分かったこと**: BANK1 に RAM バンクを出したままでは
I/O イネーブラを書き換えられない（窓ごと隠れるため）。§5.8。

### 2026-09-12 (3) — Y8960 を駆動するファームウェアが在ることが分かった

**やったこと**: madscient/MsxSoundSuiteExtension（MSSX）を読み、
`docs/y8960/hardware.md`（「本ファームウェアが依存する範囲」）の内容を
§3.3 にまとめた。**コードは書いていない。ROM も走らせていない。**

**これは情報源の格が違う。** 一次資料（RTL・xlsx・docx）は WIP で互いに
食い違っていたが、MSSX は**実際に Y8960 を駆動するソフトが前提にしている
仕様**である。`hardware-notes.md`（写し、2026-09-08 の調査）より新しく、強い。

**決着が付いたもの**（§3.3）。

- **ROM / RAM の境界** — `#0`-`#15` が ROM、`#16`-`#31` が RAM。
  §9.5 の主要な未決が解けた。**openMSX_Y8960 が保留にしていた設計差は、
  既存実装（分割して持つ）の側が正しかった**ことになる
- **SCC 窓は BANK1 の `7800h`** — 従来実装の 9800h だけではファームウェアが
  動かない。openMSX_Y8960 が 4 region に広げた判断が裏付けられた
- **OPLL の I/O Enabler は b0 = 7Ch-7Dh** — openMSX_Y8960 が RTL を
  取り違えとして採らなかった判断が裏付けられた
- **ADPCM は 2 回路で 256KB を共有** — openMSX_Y8960 とは違う（§5.1）
- **クロック** — SSGS 1.7897725MHz、他 3.579545MHz。写しには無かった値

**検証手段が一つ増えた**（§8.3）。`y8960bas.rom` (128KB) を Y8960 SCC の
マッパーに載せれば、**バンク・窓・トンネル・イネーブラ・音源が同時に通ったか**を
一度に見られる。**openMSX_Y8960 が持っていなかった手段**である。

**ライセンスの線が一本増えた**（§3.1）。MSSX の ROM は**再配布に
MSX ライセンシングコーポレーションの許諾が要る**。リポジトリに入れない。
ローカルでの検証は個人利用の範囲。

**残る最優先は §9.1（I/O ポートの重ね合わせ）。** MSSX が決着させたのは
メモリまわりであって、I/O ポートの重なりは blueMSX 側の制約なので、
外からの情報では解けない。

**写しの扱いを変えた。** `hardware-notes.md` は改変しない方針だが、
**冒頭の「blueMSX+ 側で足した節」に、古くなった記述の一覧を足した**。
写しだけを読んだ人が決着済みの論点を未決と信じるのを防ぐため。

### 2026-09-12 (2) — ビルド環境

**やったこと**: ビルドを通し、実行できる状態にした。
経緯と詰まりどころは `doc/fork/build/README.md` §7。

- **Release / x64 が 0 エラーで通る**（**確認済み**。7 警告はすべて上流由来、2 分 2 秒）
- **ヘッドレスで走る経路を見つけた**。§8.1 を書き直した
- 実機の BIOS ROM を重ね、96 機種が起動可能な状態にした

**本書の §8 を訂正した。** 「blueMSX+ に自動テストの手段が無い」は言いすぎで、
list 系のオプションはウィンドウを作らずに終わる。

### 2026-09-12 (1) — 調査と計画

**やったこと**: blueMSX+ と openMSX_Y8960 / Y8960emu / EPSGemuEngine を読み、
本書と `hardware-notes.md`（写し）、`doc/fork/README.md` を書いた。
ルートの `CLAUDE.md` は上流の `.gitignore` が除外しているため追跡されない。
**残す規則は `doc/fork/README.md` に置いた**（2026-09-12 ユーザー判断）。
**コードは一行も書いていない。ビルドも実行もしていない。**

**分かったこと**（いずれも **確認済み(読解)**）。

- OPL2EX の土台が既にある。`Src/SoundChips/OpenMsxY8950Latest/` は
  openMSX の Y8950 の移植で、openMSX_Y8960 の `Y8960OPL2` は同じ Y8950 の
  フォークである。当てる差分が既に openMSX 側にある
- OPLLEX は当初 Nuked-OPLL 案を出した。`NukedOPLL/opll.c` は
  YM2413 / YM2423 / YMF281 / DS1001 の 4 音色 ROM を**すべて持っており**、
  `patchrom` の参照が 2 か所しか無い。ユーザー判断で emu2413 を採ることになった
- **emu2413 には YM2423 (OPLL-X) の音色データが無い**（3 セットのみ）。
  §5.2 / §9.2
- **`ioPortRegister` は 1 ポート 1 デバイスで、先勝ち、失敗は黙る。**
  そして本体 PSG はスロットデバイスより**後**に登録される。
  Y8960 の SSGS をマシン構成に置くと本体 PSG が壊れる。§4.2 / §9.1
- blueMSX のミキサーにデバイス単位のゲイン API が無い。§4.3
- blueMSX+ にスクリプトによる自動テストの手段が無い。§8

**決めたこと**（すべてユーザー判断、2026-09-12）。

| | 決定 |
|---|---|
| 進め方 | 計画文書を先に作る。実装は次セッション以降 |
| OPLLEX のコア | emu2413 のフォーク 1 本 |
| ミキサー種別 | Y8960 専用を 1 つ新設 |
| RomType | 今決める（§6.1 に確定表） |

そのとき挙げた「次にやること」は §9.1 を決めることだった。**これは今も変わらない。**
