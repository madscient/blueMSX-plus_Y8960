# Makoto（YM2608 OPNA カートリッジ）対応 — 計画と経緯

対象読者は開発者と AI。Y8960 とは独立した、音源カートリッジの追加。

上流候補なので、**`upstream/develop` から切った枝 `feature/makoto-opna` に、
コードだけのコミットで作る**（`doc/fork/README.md` の「上流へ出すとき」）。
この文書は上流に出さない。

## 1. 調べて分かったこと

### 1.1 Makoto のハードウェア

出典は VGMPlay MSX の Makoto ドライバ（sharksym/vgmplay-sharksym の
`src/drivers/Makoto.asm`）。**確認済み(読解)**。製品ページ（Supersoniqs）と
MSX Resource Center の開発スレッドには I/O の記述が無い。

| | |
|---|---|
| チップ | YM2608 (OPNA) + YM3016 (DAC) |
| クロック | 8 MHz |
| I/O ポート | `14h` 表アドレス / 表ステータス、`15h` 表データ、`16h` 裏アドレス / 裏ステータス、`17h` 裏データ |
| 表（`14h`/`15h`） | FM 1–3ch、SSG、リズム（ADPCM-A）、共通レジスタ |
| 裏（`16h`/`17h`） | FM 4–6ch、ADPCM-B |
| ADPCM-B のメモリ | ドライバは **1bit DRAM モード**（裏 `01h` の bit1 = 0）に設定する。アドレスは 4 バイト単位 16bit なので上限 256 KB |
| `14h` の読み | ドライバは読まない。コメントに「Music Module とバスが衝突する」とある |
| 検出 | 裏 `00h` に `80h`（START）を書き、裏ステータスの bit5 が立つことを見る |

**利用者から得たこと**（2026-09-22）: ADPCM-B の RAM は **256 KB**、
**/IRQ は MSX の /INT に繋がっている。** どちらもドライバからは決まらなかった
（ドライバは容量を調べず、割り込みを切って使う）。

**未確認**: `14h` の読みで何が返るか（本物のステータスか、衝突で不定か）。

### 1.2 ymfm

GitHub の aaronsgiles/ymfm、`81aec25ccb`（2026-07-27）時点。**確認済み(読解)**。

| | |
|---|---|
| ライセンス | BSD-3-Clause（GPLv2 と両立する） |
| OPNA の実装 | `ymfm_opn.{h,cpp}` の `ym2608` |
| 依存 | `ymfm.h`、`ymfm_fm.{h,ipp}`、`ymfm_adpcm.{h,cpp}`、`ymfm_ssg.{h,cpp}` |
| 同居しているもの | `ymfm_opn.cpp` には OPN / OPNB / OPN2 系の他チップも入っている |

**ymfm がホストに任せているもの**（`ymfm_interface` の仮想関数）:

| | ymfm の口 | blueMSX 側で要るもの |
|---|---|---|
| リズムの波形 ROM（8 KB） | `ymfm_external_read(ACCESS_ADPCM_A, …)` | 利用者が置くファイルを読む（§2） |
| ADPCM-B の RAM | `ymfm_external_read/write(ACCESS_ADPCM_B, …)` | カートリッジが持つバッファ |
| タイマ A / B | `ymfm_set_timer` | ボードのタイマに繋ぐ。**割り込みを使わないソフトもステータスのフラグで待つ**ので要る |
| IRQ | `ymfm_update_irq` | 繋ぐかどうかは §1.1 の未確認事項による |
| 出力 | `generate()`。FM の出力は 8 MHz / 144 ≈ 55.5 kHz | ミキサーのレートへ変換する |

`ym2608::reset()` はリズム 6 音の開始・終了アドレス（`0000h`–`1FFFh`）を
自分で設定する。ROM の中身だけを渡せばよい。

### 1.3 リズム ROM

MAME は YM2608 の内蔵 ROM を `ym2608_adpcm_rom.bin`（8 KB、CRC32 `23c9e0d8`）として扱う
（`src/devices/sound/ymopn.cpp`、**確認済み(読解)**）。中身は YM2608 のマスク ROM の
吸い出しで、**再配布できない**。

### 1.4 blueMSX 側

| | |
|---|---|
| `14h`–`17h` の既存の使い手 | `romMapperSvi328RsIDE.c`（SVI-328 のみ）。I/O ポートは多重登録できるので、重なっても登録は落ちない |
| C++ の規格 | `msvc2022/blueMSX.vcxproj` はファイルごとに `LanguageStandard` を指定している（`OpenMsxY8950Latest*.cpp` が `stdcpp20`） |
| ビルド定義 | `msvc2022`、`msvc2026`、`gcc/Makefile` の 3 系統。明示列挙 |

## 2. 決まったこと（2026-09-22 ユーザー判断）

| | 決定 | 前提 |
|---|---|---|
| 枝 | `upstream/develop` から `feature/makoto-opna`。コードだけ | 上流の受け口が `develop` であること |
| 計画文書 | 本書（`feature/y8960` 側） | 上流は AI 向け文書を置かない方針 |
| リズム ROM | **利用者が置く。無ければリズムだけ無音**、他の音は鳴る | ROM は YM2608 内蔵のマスク ROM の吸い出しで、再配布できない |
| ymfm の取り込み | **必要なファイルを無改変で** `Src/SoundChips/ymfm/` に置き、取り込んだコミットを記録する | ymfm の更新にそのまま追従できることを、コード量より優先する |

### 見送ったもの

- **リズムを実装しない案** — 見送り。ROM を利用者が置けば鳴らせるので、
  鳴らせる経路を最初から閉じる理由が無い
- **`ymfm_opn.cpp` から `ym2608` だけを抜き出す案** — 見送り。
  ymfm を更新するたびに抜き出し直すことになる

## 3. 外に出る値（決める直前に聞く）

後から変えると利用者のファイルに波及するもの。

**RomType の番号は数値のまま保存される**（**確認済み(読解)**）。マシン構成の
スロット行（`Machine.c` が `toint()` で読む）、カートリッジ履歴
（`Properties.c` の `GET_INT_VALUE_2i1(histFile, media, carts, i, type)`）、
セーブステート（`Board.c` の `cartType00`）。名前は表示と `/listromtypes` にだけ使う。

### 3.1 決まったもの（2026-09-22 ユーザー判断）

| 値 | 決定 | 前提・やり直しの値段 |
|---|---|---|
| RomType の番号 | **188** | 上流の次の空きは 180 だが、このフォークの Y8960 が 180–187 を使っており、公開済みの `v3.1.1-y8960.1` に入っている。上流には PR で 180–187 の予約を頼む。断られたら定数 1 つを振り直す（上流の枝だけ。フォーク側は Y8960 を振り直すことになる） |
| ミキサーのチャンネル種別 | **`MIXER_CHANNEL_YAMAHA_SFG` と共用** | 外付け FM カートリッジとして SFG のスライダーに入れる。両方挿すと音量を別々に変えられない。後で新設に切り替えても、既存の設定キーはそのまま残り新しいキーが既定値で加わるだけなので、利用者の設定は壊れない |

見送ったもの:

- **180 を取り Y8960 を 181–188 にずらす案** — 見送り。公開済みリリースの
  利用者の `config.ini` と、`y8960/tests/` の検証用構成 6 本が壊れる
- **チャンネル種別の新設** — 見送り。`AudioMixer.h`、`Actions`、`Emulator.c`、
  `Properties.c`、`ThemeTriggers`、`Win32.c`、言語ファイル、テーマ定義の
  10 ファイル前後に波及し、上流との衝突面が広がる
- **MSX-AUDIO と共用する案** — 見送り。手間は SFG と同じで、外付け FM
  カートリッジという括りの方が利用者に分かりやすい（**推測**: 利用者が
  どちらを期待するかを測ったわけではない）

### 3.2 実装の過程で決めたもの（2026-09-22 ユーザー判断）

実装の枝に仮置きで入れた値を、利用者が確かめて決めた。

| 値 | 決定 | 置き場所 | 波及先 |
|---|---|---|---|
| 特殊カートリッジ名（`CARTNAME_MAKOTO`） | `Makoto` | `Properties.h` | 設定とカートリッジ履歴に**名前で**残る（`RomTypeList.c` の注記「must never change」） |
| RomType の表示名・短縮名 | `Makoto` / `MAKOTO` | `MediaDb.cpp` | 表示と `/listspecials`。短縮名はコマンドラインの `/romtype` で受け付ける |
| セーブステートの節名 | `ym2608` | `YM2608.cpp` | 保存済みのステート |
| リズム ROM のファイル名と置き場所 | `Machines/Shared Roms/ym2608_rhythm.rom` | `romMapperMakoto.c` | 利用者の手順。MAME は同じ中身を `ym2608_adpcm_rom.bin` と呼ぶ（§1.3） |
| ADPCM-B の RAM 容量 | 256 KB（実機と同じ） | `romMapperMakoto.c` | ステートは容量を持っており、違う容量のステートは入る分だけ読む |
| /IRQ | **繋ぐ**。割り込みの線 `1000h` | `romMapperMakoto.c`（`MAKOTO_IRQ_MASK`） | 線の番号は外に出ない。他のデバイスが使っていないことを `grep` で 2 度確かめた |
| メニュー ID | `41122` | `Win32Menu.c` | 外には出ない |

### 3.3 手本にする既存の型

**Joyrex PSG**（I/O ポートにだけ PSG が付き、特殊カートリッジとして挿す）。
現れる場所は次のとおり（**確認済み(読解)**、`grep`）。Makoto も同じ場所に足す。

`MediaDb.h`（番号）、`MediaDb.cpp`（表示名と短縮名の 2 か所）、`RomTypeList.c`、
`Properties.h`（`CARTNAME_*`）、`Properties.c`（特殊カートリッジの一覧）、
`FileHistory.c`（2 か所）、`MegaromCartridge.c`（挿入）、`Machine.c`（マシン構成）、
`romExclusion.h` と `MsxTypes.h`（除外マクロ）、`Win32Menu.c`（メニュー ID・表示・コマンド）、
`Win32machineConfig.c`（5 か所）、`Language.{c,h}` と各言語のメニュー文字列、
ビルド定義（`msvc2022`・`msvc2026`・`gcc`・`blueMSXlite/linux-sdl`）。

Makoto では**言語ファイルに触れていない。** 製品名は訳さないので、Yamanooto と同じく
`Win32Menu.c` に文字列を直接書いた。

**`blueMSXlite/linux-sdl/Makefile` には足していない。** 存在しない `Moonsound.c` を
列挙しており（341 行目）、既に組めない状態だった。`gcc/Makefile` には足したが、
こちらも上流が足した Emu2413 や NukedOPLL を載せておらず追従が止まっている。
**ビルドを確かめたのは msvc2022 だけである。**

## 4. 実装（`feature/makoto-opna`）

| コミット | 内容 |
|---|---|
| `a85d49f9` | ymfm の 10 本を無改変で `Src/SoundChips/ymfm/` に。出所は同ディレクトリの `README.md` |
| `dca372b1` | `YM2608.{h,cpp}`（ymfm とボード・ミキサーの接続）、`romMapperMakoto.{h,c}`、登録 15 ファイル |
| `2ade339f` | /IRQ を割り込みの線 `1000h` に繋ぐ（§3.2） |

### 4.1 作りの要点

| | どうしたか | なぜ |
|---|---|---|
| 忠実度 | `OPN_FIDELITY_MIN`。出力は常に clock / 48（8 MHz で 166.7 kHz） | プリスケーラの設定によらず出力レートが一定になる（`ymfm_opn.cpp` の `update_prescale` の表） |
| レート変換 | ミキサー 1 サンプルぶんのネイティブ出力を平均する。比は `clock` 対 `sampleRate * 48` の整数で数える | 誤差を貯めない。フィルタとしては箱形で粗い（**未検証**: 耳で聴いての評価はしていない） |
| 混合 | L = FM_L + SSG、R = FM_R + SSG | ymfm の `examples/vgmrender` と同じ |
| 出力の倍率 | 2 倍 | ミキサーは音量 1024 を掛けて 4096 で割る。フルスケールの OPNA がミキサーの約半分になる（**推測**: 他の音源との音量の釣り合いは聴いていない） |
| タイマ | ymfm が求めるクロック数をボード時間に換算して `boardTimerAdd`。**満了から再設定されるときは、予定時刻から数える** | ボードは命令の切れ目でタイマを発火するので、コールバックは遅れる。発火時刻から数えると周期が伸びる |
| ビジー | ymfm が求めた終了時刻をボード時間で持つ | |
| レジスタ書き込み・読み出し | 前に `mixerSync` | 音の生成を書き込み時刻まで進めてから状態を変える |
| peek | 表ステータスだけ返し、他は `FFh` | 裏ステータスの読みはフラグを書き戻し、裏データの読みは ADPCM-B のアドレスを進める（`ymfm_opn.cpp` の `read_status_hi` / `read_data_hi`） |
| ROM が無いときのリズム | ymfm に `08h` を返す | ニブル 0 と 8 が最小の段を足して引くので、アキュムレータは 0 と 2 を行き来するだけになる（`ymfm_adpcm.cpp` の `clock` の復号）。`00h` を返すとずれて行く（§4.2 の b05） |
| デバッガのレジスタ表示 | 書き込みを自前で控える | ymfm はレジスタを外に出していない |
| セーブステート | ymfm の `save_restore` のバイト列、タイマ、ビジー、レート変換の端数、ADPCM-B の RAM、レジスタの控え | |

### 4.2 確かめたこと

**ビルド**（**確認済み**）: msvc2022 の Release / x64 で 0 エラー。追加したファイルからの
警告は 0 件（6 件の警告はすべて触っていない既存ファイル）。`blueMSX+.exe` は別の実行中の
プロセスが掴んでいたので、リンク先だけを `-p:OutDir=` で作業ツリーの外に変えた。

**登録**（**確認済み**）: そのビルドの `/listspecials` に `MAKOTO  Makoto` が出た（56 行中）。
**`/listromtypes` には出ない** — ROM イメージに掛けるマッパーの一覧で、Joyrex PSG も出ない。
Makoto の登録を確かめる手段にはならない。

**ホスト上の probe**（`tests/opna-probe.cpp`、**確認済み**）: `YM2608.cpp` と ymfm を
エミュレータから切り離してスタブと組み、21 項目がすべて OK。スタブは時間を進められ、
ボードタイマを命令の切れ目の遅れ（30 µs）込みで発火する。

| 項目 | 期待値の出所 |
|---|---|
| FM の音程（F-Num 618、BLOCK 4 → 261.94 Hz） | OPN の F-Number の式（**推測**: 記憶による。ymfm からは取っていない） |
| SSG の音程（TP 284 → 440.14 Hz） | clock / (64 × TP)（同上） |
| リズム: ROM 無しで無音、ROM ありで鳴る | ROM ありで RMS 10613.6、無しで 10.4 |
| リズムがひとりでに鳴り終わる（BD 1 打、`opnatest.asm` と同じ BD/SD の並び） | 1〜1.5 秒後の RMS が 0.0 |
| タイマ A の満了（1 回目・2 回目、前後 50 µs） | ymfm の 1 ステップ 144 クロック。**グルーの時間換算だけを確かめる**（ymfm の式そのものは確かめていない） |
| IRQ: マスク 0 で `boardSetInt` が呼ばれない / `1000h` で立ち、フラグのリセットで落ちる | |
| IRQ を繋いだ状態で、リセット後に放置しても割り込みが立たない | ymfm はリセットで IRQ の許可を `1Fh`（全部）にする（`ym2608::reset`）。立てば MSX は起動中に止まる |
| ビジー: 書き込み直後に立ち、100 µs 後に落ちる | |
| ADPCM-B の書き込みが RAM に届く（1bit DRAM で開始アドレス 1 → バイト 4、8bit DRAM で → バイト 32） | VGMPlay MSX の Makoto ドライバが 1bit DRAM で 4 バイト単位を前提にしている（§1.1）。**RAM の側を直接見ている**ので、往復ではない |
| セーブしてロードすると同じ音が出る / ロードしなければ違う | |

**判別力**（**確認済み**）: `YM2608.cpp` を 12 通りに壊した版（作業ツリーの外）で、
それぞれ対応する項目が落ちた。

| 壊し方 | 落ちた項目 |
|---|---|
| b01 ボード時間への換算を半分に | タイマ A の前（1 回目・2 回目） |
| b02 再設定を発火時刻から数える | タイマ A 2 回目の後 |
| b03 出力レートの分母を 24 に | FM の音程、SSG の音程 |
| b04 ROM を渡しても使わない | リズムが鳴る |
| b05 ROM 無しで `00h` を返す | リズムが無音 |
| b06 マスク 0 でも `boardSetInt` を呼ぶ | IRQ を繋がない |
| b07 ADPCM-B の書き込みを捨てる | ADPCM-B の 3 項目 |
| b08 ビジーを常に偽に | 書き込み直後のビジー |
| b09 ロードで ymfm の状態を戻さない | セーブとロード |
| b10 ロードでレート変換の端数を戻さない | セーブとロード |
| b11 IRQ を常に立った扱いにする | フラグのリセットで落ちる |
| b12 リセットでタイマ A を動かす | リセット後に放置、タイマ A の後（1 回目・2 回目）、`1000h` で立つ |

b02 は、スタブのタイマを遅れ無しで発火させていた間は**捕まらなかった**。遅れを
入れて初めて捕まった。

b11 は「リセット後に放置」の項目では**捕まらない。** ymfm は IRQ の状態が
変わったときにしか通知しないので、放置している間は立ちっぱなしの壊れが表に出ない。
この項目の判別力は b12 で示した。

**リズム ROM**（**確認済み**）: 利用者の手元の 8192 バイトのファイルの CRC32 `23c9e0d8`、
SHA1 `50b6c3e288eaa12ad275d4f323267bb72b0445df` が、MAME の `ym2608_adpcm_rom.bin`
（`src/devices/sound/ymopn.cpp` の `ROM_LOAD16_WORD`）と一致した。ymfm がリズム 6 音に
割り当てる範囲 `0000h`–`1FFFh` とも大きさが合う。

**エミュレータの中**（`tests/opnatest.asm`、**確認済み** 2026-09-22、`2ade339f` のビルド）:
試験 ROM を slot 1、Makoto を特殊カートリッジとして slot 2 に挿し、`/hidden` で走らせて
WAV を録り、`analyze-opnatest.py` で判定した。機種は `MSX2+ - C-BIOS - JP`。6 区間すべて OK。

| 区間 | 結果 |
|---|---|
| 1 FM ch1（表のポート 14h/15h） | 261.94 Hz（期待 261.94） |
| 2 SSG | 440.15 Hz（期待 440.14） |
| 3 リズム BD/SD × 4 | 鳴った（1.01 秒の塊）。音程は判定しない |
| 4 FM ch4（裏のポート 16h/17h） | 523.89 Hz（期待 523.88） |
| 5 タイマ A をポーリングで 500 回 | 327.21 Hz、**0.910 秒**（期待 0.9） |
| 6 タイマ A の割り込みを H.KEYI で 500 回 | 586.86 Hz、**0.910 秒**（期待 0.9）。C-BIOS は H.KEYI を呼ぶ |

**判別力**（**確認済み**）: `MAKOTO_IRQ_MASK` を 0 にした版を作業ツリーの外に出力してビルドし
（ソースは直後に `git checkout` で戻した）、同じ試験で走らせた。区間 6 が録音の終わりまで
8.35 秒止まらず、4 件落ちた。

この版はデプロイ先の外にある exe だったので、**区間 3 のリズムも消えた。** blueMSX+ は
起動時に exe のあるディレクトリへ `chdir` する（**確認済み(読解)** `Win32.c` 4102-4105 行）ので、
`Machines/Shared Roms/ym2608_rhythm.rom` が見えなかった。**ROM が見えれば鳴り、見えなければ
リズムだけが消えて他は鳴る**ことが、意図せずエミュレータの中でも見えた。

### 4.3 試験のときに踏んだこと

- **試験 ROM の誤り**: 最初の版はリズムと区間 5 の回数を E / DE で数え、ループの中で
  `regs`（D と E を壊すと自分で書いた）を呼んでいた。リズムが延々と繰り返され、以降の区間が
  鳴らなかった。ホスト probe で同じ手順が鳴り終わることを確かめてから録音を詳しく見て、
  0 の区間を挟んだ打音の繰り返しだと分かった。回数はメモリに置く形に直した
- **`/romtype1 NORMAL` を付けると ROM が挿さらない。** C-BIOS が「No cartridge found」を出した
  （**確認済み**、画面）。付けなければ挿さる。原因は調べていない（**未確認**）。試験は付けずに走らせる
- 最初の数回は長いパス（約 190 文字）の ROM を渡しており、そのときも無音だった。
  **パスの長さが原因かは未確認**（その実行の画面は撮っていない）。試験は `%TEMP%` の下の短いパスに置く
- 録音が全部 0 のとき、エミュレータが動いていないのか ROM が挿さっていないのかは録音からは
  分からない。本体の PSG で目印を鳴らす版と、メニューからのスクリーンショットで切り分けた

### 4.4 確かめずに残してあること

- 実機の BIOS の機種（HB-F1XDJ+ など）での試験。エミュレータの中の試験は C-BIOS だけ
- マシン構成のスロットに置いた場合（特殊カートリッジとしては挿した）
- 実機のソフト（VGMPlay MSX など）での再生
- 音量の釣り合い、レート変換の音質（人が聴いていない）
- ADPCM-B の再生（probe は RAM への書き込みまで）
- OPNA の割り込みを許可しながら処理ルーチンを持たないソフトの挙動（実機と同じく割り込みが止まらなくなるはず）
- FM と SSG の期待値の式が YM2608 の原典と一致すること（記憶による。エミュレータとホストで一致しているが、どちらも ymfm なので独立ではない）
- `gcc/Makefile` と msvc2026 でのビルド

## 5. リリース

**公開中は `v3.1.1-makoto.2`**（pre-release、`feature/makoto-opna` の `b2d4d185`）。
`v3.1.1-makoto.1`（`2ade339f`）はライセンス表示を欠いていたので**下書き（draft）に戻した**
（2026-09-22 ユーザー指示）。GitHub のリリースに非公開の設定は無く、下書きが一般から隠す手段。
タグは残る。タグ名は Y8960 と同じ形（2026-09-22 ユーザー判断）。確かめたことは
`doc/fork/README.md` の「リリースの記録」。

| | |
|---|---|
| AI 向け文書 | 枝にもともと無い（`upstream/develop` からコードだけで作った）ので、`.gitattributes` の手当ては要らない |
| 使い方 | リリースノートに直接書いた。枝は上流候補なので利用者向け文書を置かない |
| リズム ROM | 同梱しない。**利用者が自分で入手する**旨をリリースノートの「同梱していないもの」に書いた（2026-09-22 ユーザー指示） |

**ライセンス表示**: makoto.1 の zip にはライセンス文書が 1 つも無かった（ymfm の BSD-3-Clause も、
blueMSX+ 本体の GPL も。上流の配布物に共通の状態）。`scripts/ci-package.ps1` は `ReleaseFiles/` を
丸ごと入れるので、**`ReleaseFiles/LICENSE.txt` と `ReleaseFiles/Licenses/` を足して**（`b2d4d185`）
makoto.2 を出した。載せたものと理由:

| 文書 | 対象 | なぜ要るか |
|---|---|---|
| `GPL-2.0.txt`（gnu.org の公式テキスト） | blueMSX+ 全体、openMSX・Nuked OPLL・Scale2x 由来 | GPLv2 第 1 条は受け手に本文を渡すことを求める |
| `LGPL-2.1.txt`（同） | `Src/Utils/blowfish.c`（Paul Kocher） | LGPL の条件 |
| `Emu2413-MIT.txt` / `Emu8950-MIT.txt` | Mitsutaka Okazaki | MIT は複製物に表示を求める |
| `ymfm-BSD-3-Clause.txt` | Aaron Giles | BSD-3 第 2 条 |
| `blueMSX+-BSD-3-Clause.txt` | Hesoten の 6 本（`SdCard`、`AnalogFilter`、`Utf8Conv.h`、`Win32MediaFoundation`、`Win32TextUtf8.h`、`Win32Toast`） | 同上 |
| `WinPcap-BSD-3-Clause.txt` | `ThirdParty/WinPCap/Packet32.h`（`Win32Eth.c` が取り込む） | 同上 |

ビルドに入っているかは vcxproj を XML として読んで確かめた（**確認済み**）。`Midi_w32.c`（BSD）は
プロジェクトに無いので載せていない。**`grep` で数えた最初の判定は取りこぼしていた**
（`Win32Toast.cpp` を「無い」と出した）ので、項目の出入りは XML で決めること。

表示を求めないので名前だけ挙げたもの: TinyXML・zlib/minizip（zlib 型）。MAME 由来の
`MameYM2151.c`・`MameVLM5030.c`・`Fmopl.c`・`Ymdeltat.c` は**ヘッダにライセンスの記述が無い**
（`license.txt` は Fmopl / Ymdeltat を「fMSX-SDL - GPL」とするが、ヘッダと食い違う。**未確認**）ので、
作者の表示だけにした。**元の blueMSX のコードを zlib 型と書くのは誤り** — `license.txt` 自身が
「多くは GPLv2 のヘッダを持つ」と注記している。

`LICENSE.txt` はフォークの名前を書かず、「ソースは配布元のページで一緒に公開」とした。
`b2d4d185` は上流にもそのまま出せる形である（上流の zip も同じ状態のため）。

**Y8960 の `v3.1.1-y8960.1` も同じくライセンス文書を欠いていた**（**確認済み**: zip を開いた）。
`b2d4d185` を `feature/y8960` に `-x` で取り込み（`b5a7cf13`）、`LICENSE.txt` を Y8960 に合わせて直して
（`337c23a3`）`v3.1.1-y8960.2` を出し、y8960.1 を下書きに戻した（2026-09-22 ユーザー指示。中身は
y8960.1 に揃えず、今の `feature/y8960` 全体とした）。Y8960 側の `LICENSE.txt` の違い:

- **ymfm の行は残した。** OPLLEX のリズム 3 音は ymfm の YM2413 の表から取っている（`Y8960OpllCore.c` の注記）
- **OPLLEX の音色データの表示を足した。** "Copyright free OPLL(x) ROM patches"（David Viens、Hubert Lamontagne）、
  CC BY-SA。**出典の wiki は版を書いていない**（**確認済み**: 2026-09-22 に wiki を読んだ）ので、本文は同梱せず、
  題名・作者・URL・「版の記載なし」を書いた
- Emu2413 の行に、OPLLEX のコアの土台であることを書いた

**`LICENSE.txt` は 2 つの枝で中身が違う。** 上流に出すのは Makoto 側（`b2d4d185`）の形で、Y8960 側はそこに
Y8960 の分を足したもの。上流が採ったら、`develop` のマージで Y8960 側の差分が衝突しうる。

**メニューとマシン構成エディタからの挿入は画面で試していない**（**確認済み(読解)**: Joyrex PSG と
同じ経路に足した）。エミュレータの中で試したのは `/special2 MAKOTO` だけ。

## 6. 経緯

- 2026-09-22 調査と §2 の決定。
- 2026-09-22 §3.1 の決定。実装の枝 `feature/makoto-opna` を `upstream/develop` `a07b26f4` から切った。誤って上流へ push しないよう追跡先は外してある。
- 2026-09-22 利用者からリズム ROM の所在を受け取った（ローカルのパスはここに書かない）。§4 の実装と確認。§3.2 の値は仮置き。
- 2026-09-22 利用者が RAM 容量（256 KB）と /IRQ の配線（繋がっている）を示し、§3.2 の残りを確定した。/IRQ を繋ぎ、リセット後に割り込みが立たないことを probe に足した。
- 2026-09-22 利用者が Makoto 専用のデプロイ先を用意した（ローカルのパスはここに書かない）。exe を差し替え、リズム ROM を `Machines/Shared Roms/` に置いた。差し替え前の exe は同じ場所に別名で残してある。§4.2 のエミュレータでの試験。
- 2026-09-22 両方の枝を push し、`v3.1.1-makoto.1` を公開した（§5）。リズム ROM を利用者が入手する旨をリリースノートに足した（ユーザー指示）。
- 2026-09-22 配布 zip にライセンス文書が無いことを利用者と確かめ、`ReleaseFiles/` に置いて `v3.1.1-makoto.2` を出し、makoto.1 を下書きに戻した（ユーザー指示）。
- 2026-09-22 Y8960 側も同様に対処した（ユーザー指示）: ライセンス文書を足して `v3.1.1-y8960.2` を出し、y8960.1 を下書きに戻した。
