# Per-user IDE settings (Visual Studio generates these automatically).
*.vcxproj.user

# MSBuild intermediate / output directories.
[Dd]ebug/
[Rr]elease/
[Ff]inal/
x64/
*.tlog/
*.opensdf
*.sdf
*.suo
.vs/

# Claude Code local state and per-session working files.
.claude/
CLAUDE.md
# This fork keeps its AI-facing rules in the repository, unlike upstream.
# Only the root index is tracked; the rules themselves are in doc/fork/ai/.
!CLAUDE.md
tmp/
